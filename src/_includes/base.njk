<!doctype html>
{% set pageLocale = locale or site.locale or 'pl' %}
{% set isPolish = pageLocale == 'pl' %}
{% set homeUrl = '/' %}
{% set consultingUrl = '/pl/wdrozenia-ai-dla-firm/' if isPolish else '/services/ai-solutions-development/' %}
{% set roadmapUrl = '/pl/wdrozenia-ai-dla-firm/' if isPolish else '/services/ai-solutions-development/' %}
{% set homeLabel = 'Strona główna' if isPolish else 'Home' %}
{% set loginLabel = 'Logowanie' if isPolish else 'Log in' %}
{% set signupLabel = 'Rejestracja' if isPolish else 'Sign up' %}
{% set servicesLabel = 'Produkt' if isPolish else 'Product' %}
{% set howItWorksLabel = 'Jak to działa' if isPolish else 'How It Works' %}
{% set aboutLabel = 'Dlaczego ten system' if isPolish else 'Why this system' %}
{% set menuLabel = 'Menu' if isPolish else 'Menu' %}
{% set langSwitchLabel = 'EN' if isPolish else 'PL' %}
{% set langSwitchText = 'English' if isPolish else 'Polski' %}
{% set langSwitchHint = 'Wersja językowa' if isPolish else 'Language' %}
{% set isHome = page.url == "/" %}
{% set isServicesIndex = page.url == "/services/" %}
{% set isServices = page.url and page.url.indexOf("/services/") == 0 %}
{% set isBlog = false %}
{% set isBlogIndex = false %}
{% set isLegal = page.url == "/privacy-policy/" or page.url == "/cookie-policy/" or page.url == "/pl/polityka-prywatnosci/" or page.url == "/pl/polityka-cookies/" %}
{% set isStart = page.url == "/start-with-ai/" or page.url == "/implementation-process/" or page.url == "/business-ai-faq/" %}
{% set isContact = page.url == "/contact-us/" or page.url == "/pl/kontakt/" %}
{% set isWhyUs = page.url == "/why-us/" %}
{% set isLeadPlan = page.url == "/services/ai-solutions-development/" %}
{% set isContent = not isHome %}
{% set alternates = {} %}
{% set canonicalPath = canonicalPath or page.url %}
{% set canonicalUrl = (site.url + canonicalPath) if site and site.url and canonicalPath else canonicalPath %}
{% set organization = site.organization or {} %}
{% set organizationId = (site.url + '#organization') if site and site.url else '#organization' %}
{% set organizationLogo = (site.url + organization.logo) if site and site.url and organization.logo else organization.logo %}
{% set isPrimaryServicePage = page.url == '/' or page.url == '/services/ai-solutions-development/' %}
{% set breadcrumbHomeUrl = homeUrl %}
{% set breadcrumbHomeLabel = homeLabel %}
{% set languageSwitchUrl = '' %}
{% set loginUrl = '/login' %}
{% set signupUrl = '/register' %}
<html lang="{{ pageLocale }}">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <script>
      document.documentElement.classList.add("js");
    </script>
    {% set metaTitle = title or "System Jawności Wynagrodzeń" %}
    {% set pageHeading = heading or metaTitle %}
    {% set metaDescription = description or "System Jawności Wynagrodzeń pomaga firmom uporządkować płace i przygotować się na transparentność wynagrodzeń." %}
    {% set defaultFeaturedImage = "/images/favicon.png" %}
    {% set effectiveImage = image %}
    {% set effectiveImageAlt = imageAlt %}
    {% set effectiveImageWidth = imageWidth %}
    {% set effectiveImageHeight = imageHeight %}
    {% if not effectiveImage and pageType != "article" %}
      {% set effectiveImage = defaultFeaturedImage %}
      {% set effectiveImageAlt = "System Jawności Wynagrodzeń" %}
      {% set effectiveImageWidth = 512 %}
      {% set effectiveImageHeight = 512 %}
    {% endif %}
    <title>{{ metaTitle | escape }}</title>
    <meta name="description" content="{{ metaDescription | escape }}" />
    <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" />
    <meta http-equiv="content-language" content="{{ pageLocale }}" />
    <meta name="language" content="{{ pageLocale }}" />
    {% if canonicalUrl %}
      <link rel="canonical" href="{{ canonicalUrl }}" />
      <meta property="og:url" content="{{ canonicalUrl }}" />
    {% endif %}
    {% if site and site.url and site.url.length %}
      <link rel="alternate" hreflang="x-default" href="{{ site.url }}{{ canonicalPath }}" />
    {% endif %}
    <meta property="og:site_name" content="{{ site.name if site and site.name else 'System Jawności Wynagrodzeń' }}" />
    <meta property="og:title" content="{{ metaTitle | escape }}" />
    <meta property="og:description" content="{{ metaDescription | escape }}" />
    <meta property="og:type" content="{% if pageType == 'article' %}article{% else %}website{% endif %}" />
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-J7383MC8D6"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-J7383MC8D6');
    </script>
    {% if pageType == 'article' and date %}
      <meta property="article:published_time" content="{{ date.toISOString() }}" />
    {% endif %}
    {% if effectiveImage %}
      {% set ogImageUrl = (site.url + effectiveImage) if site and site.url else effectiveImage %}
      <meta property="og:image" content="{{ ogImageUrl | escape }}" />
      <meta name="twitter:image" content="{{ ogImageUrl | escape }}" />
      {% if effectiveImageAlt %}
        <meta property="og:image:alt" content="{{ effectiveImageAlt | escape }}" />
        <meta name="twitter:image:alt" content="{{ effectiveImageAlt | escape }}" />
      {% endif %}
      {% if effectiveImageWidth %}
        <meta property="og:image:width" content="{{ effectiveImageWidth }}" />
      {% endif %}
      {% if effectiveImageHeight %}
        <meta property="og:image:height" content="{{ effectiveImageHeight }}" />
      {% endif %}
    {% endif %}
    <meta name="twitter:card" content="{% if effectiveImage %}summary_large_image{% else %}summary{% endif %}" />
    <link rel="icon" type="image/png" href="/images/favicon.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
    {% if site and site.url and site.url.length %}
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Organization",
      "@id": {{ organizationId | json | safe }},
      "name": {{ organization.name | default(site.name) | json | safe }},
      "url": {{ site.url | json | safe }},
      {% if organizationLogo %}"logo": {{ organizationLogo | json | safe }},{% endif %}
      {% if organization.email %}"email": {{ organization.email | json | safe }},{% endif %}
      {% if organization.telephone %}"telephone": {{ organization.telephone | json | safe }},{% endif %}
      {% if organization.sameAs and organization.sameAs.length %}"sameAs": {{ organization.sameAs | json | safe }},{% endif %}
      "address": {
        "@type": "PostalAddress",
        {% if organization.addressLocality %}"addressLocality": {{ organization.addressLocality | json | safe }},{% endif %}
        {% if organization.addressRegion %}"addressRegion": {{ organization.addressRegion | json | safe }},{% endif %}
        {% if organization.addressCountry %}"addressCountry": {{ organization.addressCountry | json | safe }}{% else %}"addressCountry": "PL"{% endif %}
      },
      "areaServed": [
        { "@type": "City", "name": "Warsaw" },
        { "@type": "Country", "name": "Poland" }
      ],
      "contactPoint": [{
        "@type": "ContactPoint",
        {% if organization.telephone %}"telephone": {{ organization.telephone | json | safe }},{% endif %}
        {% if organization.email %}"email": {{ organization.email | json | safe }},{% endif %}
        "contactType": "sales",
        "availableLanguage": ["Polish"]
      }]
    }
    </script>
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebSite",
      "@id": {{ (site.url + '#website') | json | safe }},
      "url": {{ site.url | json | safe }},
      "name": {{ site.name | json | safe }},
      "description": {{ site.description | json | safe }},
      "publisher": {
        "@id": {{ organizationId | json | safe }}
      },
      "inLanguage": {{ pageLocale | json | safe }}
    }
    </script>
    {% if isPrimaryServicePage %}
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "ProfessionalService",
      "@id": {{ (canonicalUrl + '#service') | json | safe }},
      "name": {{ pageHeading | json | safe }},
      "url": {{ canonicalUrl | json | safe }},
      "description": {{ metaDescription | json | safe }},
      "provider": {
        "@id": {{ organizationId | json | safe }}
      },
      "serviceType": {{ pageHeading | json | safe }},
      "areaServed": [
        { "@type": "City", "name": "Warsaw" },
        { "@type": "Country", "name": "Poland" }
      ],
      "availableLanguage": ["Polish"]
    }
    </script>
    {% endif %}
    {% if isContact %}
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "ContactPage",
      "@id": {{ (canonicalUrl + '#contact-page') | json | safe }},
      "url": {{ canonicalUrl | json | safe }},
      "name": {{ pageHeading | json | safe }},
      "description": {{ metaDescription | json | safe }},
      "about": {
        "@id": {{ organizationId | json | safe }}
      }
    }
    </script>
    {% endif %}
    {% if faqSchema and faqSchema.length %}
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": [
        {% for item in faqSchema %}
        {
          "@type": "Question",
          "name": {{ item.question | json | safe }},
          "acceptedAnswer": {
            "@type": "Answer",
            "text": {{ item.answer | json | safe }}
          }
        }{% if not loop.last %},{% endif %}
        {% endfor %}
      ]
    }
    </script>
    {% endif %}
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "BreadcrumbList",
      "itemListElement": [
        {
          "@type": "ListItem",
          "position": 1,
          "name": {{ breadcrumbHomeLabel | json | safe }},
          "item": {{ (site.url + breadcrumbHomeUrl) | json | safe }}
        }
        {% if isServices and not isServicesIndex %}
        ,{
          "@type": "ListItem",
          "position": 2,
          "name": "Product",
          "item": {{ (site.url + '/services/') | json | safe }}
        }
        ,{
          "@type": "ListItem",
          "position": 3,
          "name": {{ pageHeading | json | safe }},
          "item": {{ canonicalUrl | json | safe }}
        }
        {% else %}
        ,{
          "@type": "ListItem",
          "position": 2,
          "name": {{ pageHeading | json | safe }},
          "item": {{ canonicalUrl | json | safe }}
        }
        {% endif %}
      ]
    }
    </script>
    {% endif %}
    <style>
      :root {
        color-scheme: light;
        --cc-bg: #F8FAFC;
        --cc-surface: #FFFFFF;
        --cc-sidebar-surface: #F1F5F9;
        --cc-section-surface: #F8FAFC;
        --app-stage-bg-rgb: 248, 250, 252;
        --cc-border: #E5E7EB;
        --brand-navy: #00296b;
        --brand-blue-deep: #003f88;
        --brand-blue: #00509d;
        --brand-gold: #fdc500;
        --brand-gold-bright: #ffd500;
        --brand-navy-rgb: 0, 41, 107;
        --brand-blue-rgb: 0, 80, 157;
        --brand-gold-rgb: 253, 197, 0;
        --brand-gold-bright-rgb: 255, 213, 0;
        --cc-primary: var(--brand-blue);
        --cc-primary-hover: #003f88;
        --cc-success: var(--brand-gold-bright);
        --cc-warning: #D97706;
        --cc-error: #DC2626;
        --cc-text: #0A0A0A;
        --cc-text-secondary: #64748B;
        --cc-muted: #94A3B8;
        --cc-hover: #F1F5F9;
        --cc-selected: #F8FAFF;
        --cc-progress-bg: #E5E7EB;
        --button-shadow: none;
        --button-shadow-hover: none;
        --brand-green: var(--brand-blue);
        --brand-green-soft: var(--brand-blue-deep);
        --brand-green-dark: var(--brand-navy);
        --brand-green-rgb: 0, 80, 157;

        --bg: var(--cc-bg);
        --surface: var(--cc-surface);
        --surface-soft: var(--cc-sidebar-surface);
        --border: var(--cc-border);
        --text: var(--cc-text);
        --muted: var(--cc-text-secondary);
        --muted-soft: var(--cc-muted);
        --primary: var(--cc-primary);
        --primary-soft: var(--brand-green-soft);
        --primary-dark: var(--cc-primary);
        --primary-rgb: var(--brand-green-rgb);
        --accent-tech: var(--brand-navy);
        --accent-tech-dark: var(--brand-navy);
        --accent-tech-rgb: var(--brand-navy-rgb);
        --highlight: var(--brand-gold-bright);
        --highlight-soft: var(--brand-gold);
        --highlight-rgb: var(--brand-gold-bright-rgb);
        --ring: rgba(var(--brand-green-rgb), 0.28);
        --shadow: var(--button-shadow);
        --radius: 16px;
        --radius-sm: 12px;
        --radius-full: 999px;
        --btn-radius: 999px;
        --btn-height: 42px;
        --container: 1120px;
        --header-height: 76px;
        --space-2xs: 8px;
        --space-xs: 12px;
        --space-sm: 16px;
        --space-md: 24px;
        --space-lg: 32px;
        --space-xl: 48px;
        --space-2xl: 64px;
        --space-section: clamp(64px, 7vw, 88px);
        --space-section-compact: clamp(44px, 5.5vw, 64px);
      }

      * {
        box-sizing: border-box;
      }

      html,
      body {
        margin: 0;
        padding: 0;
      }

      html {
        scroll-behavior: smooth;
      }

      body {
        font-family: "Inter", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont,
          "Segoe UI", "SF Pro Text", "Helvetica Neue", Arial, sans-serif;
        background: var(--bg);
        color: var(--text);
        -webkit-font-smoothing: antialiased;
        text-rendering: optimizeLegibility;
      }

      a {
        color: var(--accent-tech-dark);
        text-decoration: none;
      }

      a:hover {
        text-decoration: underline;
      }

      .material-symbols-rounded {
        width: 1.05em;
        height: 1.05em;
        vertical-align: -0.16em;
        stroke: currentColor;
        fill: none;
        user-select: none;
        color: currentColor;
      }

      .btn .material-symbols-rounded,
      .mobile-nav-cta .material-symbols-rounded {
        margin-right: 2px;
      }

      .btn--icon-right .material-symbols-rounded {
        margin-right: 0;
        margin-left: 2px;
        vertical-align: 0;
      }

      .btn-icon {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 14px;
        height: 14px;
        flex: 0 0 14px;
      }

      .btn-icon svg {
        width: 14px;
        height: 14px;
        display: block;
        stroke: currentColor;
      }

      .btn-icon--portal {
        width: 18px;
        height: 18px;
        flex: 0 0 18px;
      }

      .btn-icon--portal svg {
        width: 18px;
        height: 18px;
      }

      .btn-icon--roadmap {
        width: 15px;
        height: 15px;
        flex: 0 0 15px;
        background-color: currentColor;
        -webkit-mask-image: url("/images/icons/material-symbols/ai-roadmap.svg");
        mask-image: url("/images/icons/material-symbols/ai-roadmap.svg");
        -webkit-mask-repeat: no-repeat;
        mask-repeat: no-repeat;
        -webkit-mask-position: center;
        mask-position: center;
        -webkit-mask-size: contain;
        mask-size: contain;
      }

      .btn .btn-icon--roadmap {
        margin-right: 2px;
      }

      .btn-see-services {
        gap: 7px;
        padding: 10px 14px 10px 16px;
        letter-spacing: -0.005em;
      }

      .skip-link {
        position: absolute;
        left: 16px;
        top: 10px;
        padding: 10px 14px;
        border-radius: var(--radius-full);
        background: var(--surface);
        border: 1px solid var(--border);
        color: var(--text);
        box-shadow: var(--shadow);
        transform: translateY(-160%);
        transition: transform 180ms ease;
        z-index: 100;
      }

      .skip-link:focus {
        transform: translateY(0);
      }

      .cookie-banner {
        position: fixed;
        left: 16px;
        right: 16px;
        bottom: 16px;
        z-index: 200;
        border-radius: var(--radius);
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(255, 255, 255, 0.92);
        backdrop-filter: blur(10px);
        box-shadow: 0 18px 40px rgba(2, 6, 23, 0.12);
      }

      .cookie-banner[hidden] {
        display: none;
      }

      .cookie-fab {
        position: fixed;
        left: 16px;
        bottom: 16px;
        z-index: 190;
        width: 44px;
        height: 44px;
        border-radius: 999px;
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(255, 255, 255, 0.92);
        backdrop-filter: blur(10px);
        box-shadow: 0 18px 40px rgba(2, 6, 23, 0.12);
        display: inline-flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        color: var(--text);
      }

      .cookie-fab:hover {
        background: rgba(255, 255, 255, 0.98);
      }

      .cookie-fab:focus-visible {
        outline: 3px solid var(--ring);
        outline-offset: 2px;
      }

      .cookie-fab .material-symbols-rounded {
        font-size: 22px;
        vertical-align: 0;
      }

      .cookie-fab[hidden] {
        display: none;
      }

      .cookie-inner {
        display: grid;
        grid-template-columns: minmax(0, 1fr);
        gap: 12px;
        padding: 14px 14px;
      }

      .cookie-title {
        margin: 0 0 6px;
        font-size: 13px;
        font-weight: 850;
        letter-spacing: -0.01em;
        color: var(--text);
      }

      .cookie-text {
        margin: 0;
        color: var(--muted);
        font-size: 12px;
        line-height: 1.7;
        max-width: 92ch;
      }

      .cookie-settings {
        margin-top: 12px;
        display: grid;
        gap: 10px;
      }

      .cookie-settings[hidden] {
        display: none;
      }

      .cookie-setting {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        padding: 10px 12px;
        border-radius: 14px;
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(248, 250, 252, 0.72);
      }

      .cookie-setting-title {
        margin: 0;
        color: var(--text);
        font-size: 12px;
        font-weight: 850;
        letter-spacing: -0.01em;
      }

      .cookie-setting-desc {
        margin: 2px 0 0;
        color: var(--muted);
        font-size: 12px;
        line-height: 1.55;
      }

      .cookie-switch {
        position: relative;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 44px;
        height: 26px;
        border-radius: 999px;
        border: 1px solid rgba(148, 163, 184, 0.75);
        background: rgba(226, 232, 240, 0.75);
        box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.35);
        flex: 0 0 auto;
      }

      .cookie-switch input {
        position: absolute;
        inset: 0;
        opacity: 0;
        cursor: pointer;
      }

      .cookie-switch-thumb {
        width: 20px;
        height: 20px;
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.95);
        border: 1px solid rgba(148, 163, 184, 0.45);
        transform: translateX(-9px);
        transition: transform 160ms ease, border-color 160ms ease;
        box-shadow: 0 10px 20px rgba(2, 6, 23, 0.14);
      }

      .cookie-switch.is-on .cookie-switch-thumb,
      .cookie-switch input:checked + .cookie-switch-thumb {
        transform: translateX(9px);
        border-color: rgba(15, 23, 42, 0.85);
      }

      .cookie-switch.is-on {
        border-color: rgba(15, 23, 42, 0.85);
        background: rgba(15, 23, 42, 0.92);
      }

      .cookie-switch input:focus-visible + .cookie-switch-thumb {
        outline: 3px solid var(--ring);
        outline-offset: 2px;
      }

      .cookie-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        align-items: center;
      }

      .cookie-link {
        color: var(--muted);
        font-weight: 700;
        font-size: 12px;
      }

      .cookie-link:hover {
        color: var(--text);
      }

      @media (min-width: 860px) {
        .cookie-inner {
          grid-template-columns: minmax(0, 1fr) auto;
          align-items: center;
        }

        .cookie-actions {
          justify-content: flex-end;
        }
      }

      .container {
        width: 100%;
        max-width: var(--container);
        margin: 0 auto;
        padding: 0 clamp(14px, 2vw, 20px);
      }

      .site {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      .site-header {
        position: sticky;
        top: 0;
        z-index: 50;
        background: rgba(248, 250, 252, 0.84);
        border-bottom: 1px solid rgba(226, 232, 240, 0.9);
        backdrop-filter: blur(14px);
        -webkit-backdrop-filter: blur(14px);
      }

      .site-header--overlay {
        background: rgba(248, 250, 252, 0.92);
        border-bottom: 1px solid rgba(226, 232, 240, 0.9);
        backdrop-filter: blur(14px);
        -webkit-backdrop-filter: blur(14px);
      }

      .header-inner {
        display: flex;
        align-items: center;
        justify-content: flex-start;
        gap: 16px;
        min-height: var(--header-height);
        position: relative;
      }

      .brand {
        display: flex;
        align-items: center;
        color: inherit;
      }

      .brand-wordmark {
        display: inline-block;
        font-size: clamp(18px, 1.35vw, 22px);
        font-weight: 900;
        line-height: 0.95;
        letter-spacing: -0.055em;
        color: #0f172a;
        text-wrap: balance;
        max-width: 12ch;
      }

      .brand:hover {
        text-decoration: none;
      }

      .brand-logo {
        height: 32px;
        display: flex;
        align-items: center;
      }

      .brand-logo img {
        height: 100%;
        width: auto;
        max-width: 220px;
        display: block;
      }

      .nav {
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .nav-item {
        position: relative;
        display: inline-flex;
        align-items: center;
      }

      .nav-link {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 9px 12px;
        border-radius: var(--radius-full);
        font-size: 13px;
        font-weight: 650;
        color: var(--muted);
        text-decoration: none;
      }

      .nav-item--mega .nav-link {
        gap: 6px;
      }

      .nav-chevron {
        display: inline-flex;
        align-self: center;
        width: 12px;
        height: 12px;
        transform: translateY(0.5px) rotate(0deg);
        transform-origin: 50% 50%;
        opacity: 0.62;
        transition:
          transform 240ms cubic-bezier(0.22, 1, 0.36, 1),
          opacity 200ms ease-in-out;
      }

      .nav-chevron svg {
        width: 100%;
        height: 100%;
        display: block;
        stroke: currentColor;
      }

      .nav-item--mega::after {
        content: "";
        position: absolute;
        left: 0;
        right: 0;
        top: 100%;
        height: 12px;
      }

      .nav-link:hover {
        color: var(--text);
        background: rgba(226, 232, 240, 0.65);
        text-decoration: none;
      }

      .nav-link.is-active {
        background: rgba(var(--accent-tech-rgb), 0.14);
        color: var(--accent-tech-dark);
      }

      .nav-item--mega .nav-link.is-active {
        background: rgba(226, 232, 240, 0.62);
        color: var(--text);
      }

      .nav-item--mega:hover .nav-chevron,
      .nav-item--mega:focus-within .nav-chevron {
        transform: translateY(0.5px) rotate(180deg);
        opacity: 1;
      }

      .mega-menu {
        position: fixed;
        top: calc(var(--header-height) - 1px);
        left: 50%;
        width: calc(100vw - 28px);
        max-width: 1560px;
        padding: var(--space-sm);
        border-radius: var(--radius);
        border: 1px solid rgba(148, 163, 184, 0.26);
        background: rgba(255, 255, 255, 0.98);
        box-shadow:
          0 18px 36px rgba(2, 6, 23, 0.10),
          0 2px 6px rgba(2, 6, 23, 0.04);
        backdrop-filter: blur(6px);
        opacity: 0;
        visibility: hidden;
        transform: translate(-50%, 6px) scale(0.985);
        transition:
          opacity 240ms ease-in-out,
          transform 280ms cubic-bezier(0.22, 1, 0.36, 1),
          visibility 0s linear 220ms;
        z-index: 80;
      }

      .mega-menu::before {
        content: "";
        position: absolute;
        left: 14px;
        right: 14px;
        top: 0;
        height: 2px;
        border-radius: 2px;
        background: linear-gradient(90deg, rgba(var(--accent-tech-rgb), 0.66), rgba(var(--primary-rgb), 0.44));
        pointer-events: none;
      }

      .nav-item--mega:hover .mega-menu,
      .nav-item--mega:focus-within .mega-menu,
      .nav-item--mega .mega-menu:hover,
      .nav-item--mega .mega-menu:focus-within {
        opacity: 1;
        visibility: visible;
        transform: translate(-50%, 0) scale(1);
        transition-delay: 0s, 0s, 0s;
      }

      .mega-menu-head {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: var(--space-xs);
        margin-bottom: var(--space-sm);
        padding: 0 4px;
        border-bottom: 1px solid rgba(226, 232, 240, 0.9);
        padding-bottom: var(--space-xs);
      }

      .mega-menu-quickstart {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 10px;
        margin-bottom: var(--space-sm);
      }

      .mega-quick-link {
        display: grid;
        gap: 4px;
        padding: 11px 12px;
        border-radius: 10px;
        border: 1px solid rgba(226, 232, 240, 0.92);
        background: rgba(248, 250, 252, 0.84);
        color: var(--text);
        transition: border-color 170ms ease, background 170ms ease, transform 170ms ease;
      }

      .mega-quick-link:hover {
        text-decoration: none;
        border-color: rgba(var(--accent-tech-rgb), 0.3);
        background: rgba(241, 245, 249, 0.95);
        transform: translateY(-1px);
      }

      .mega-quick-title {
        font-size: 12px;
        font-weight: 780;
        letter-spacing: -0.01em;
        color: var(--text);
      }

      .mega-quick-desc {
        font-size: 11px;
        line-height: 1.45;
        color: var(--muted);
      }

      .mega-menu-title {
        margin: 0;
        font-size: 16px;
        font-weight: 850;
        letter-spacing: -0.01em;
        color: var(--text);
      }

      .mega-menu-subtitle {
        margin: 3px 0 0;
        font-size: 13px;
        line-height: 1.45;
        color: var(--muted);
      }

      .mega-menu-all {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 8px 12px;
        border-radius: 999px;
        border: 1px solid rgba(var(--accent-tech-rgb), 0.35);
        background: rgba(var(--accent-tech-rgb), 0.08);
        color: var(--accent-tech-dark);
        font-size: 12px;
        font-weight: 750;
      }

      .mega-menu-all:hover {
        text-decoration: none;
        background: rgba(var(--accent-tech-rgb), 0.14);
      }

      .mega-menu-groups {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: var(--space-sm);
      }

      .mega-group {
        display: grid;
        gap: 10px;
        padding: 14px;
        border-radius: 12px;
        border: 1px solid rgba(226, 232, 240, 0.92);
        background: rgba(255, 255, 255, 1);
        min-height: 100%;
        box-shadow: 0 2px 8px rgba(2, 6, 23, 0.03);
        transition: transform 180ms ease, box-shadow 180ms ease, border-color 180ms ease;
      }

      .mega-group:nth-child(odd) {
        background: linear-gradient(180deg, rgba(var(--accent-tech-rgb), 0.028), rgba(255, 255, 255, 1) 50%);
      }

      .mega-group:nth-child(even) {
        background: linear-gradient(180deg, rgba(var(--primary-rgb), 0.032), rgba(255, 255, 255, 1) 52%);
      }

      .mega-group:hover {
        transform: translateY(-1px);
        border-color: rgba(var(--accent-tech-rgb), 0.32);
        box-shadow: 0 10px 20px rgba(2, 6, 23, 0.06);
      }

      .mega-group-heading {
        display: grid;
        gap: 3px;
        color: var(--text);
        padding-bottom: 8px;
        border-bottom: 1px solid rgba(226, 232, 240, 0.8);
        transition: border-color 170ms ease;
      }

      .mega-group-heading:hover {
        text-decoration: none;
      }

      .mega-group:hover .mega-group-heading {
        border-bottom-color: rgba(var(--accent-tech-rgb), 0.28);
      }

      .mega-group-heading::after {
        content: "";
        width: 26px;
        height: 2px;
        border-radius: 2px;
        background: rgba(var(--accent-tech-rgb), 0.55);
        margin-top: 3px;
      }

      .mega-group-title {
        font-size: 14px;
        font-weight: 800;
        letter-spacing: -0.01em;
        color: var(--text);
      }

      .mega-group-kicker {
        font-size: 12px;
        font-weight: 650;
        line-height: 1.45;
        color: var(--muted);
      }

      .mega-list {
        list-style: none;
        margin: 0;
        padding: 0;
        display: grid;
        gap: 4px;
      }

      .mega-list a {
        display: block;
        padding: 7px 8px;
        border-radius: 8px;
        font-size: 13px;
        line-height: 1.4;
        color: var(--muted);
        border: 1px solid transparent;
        transition: background 160ms ease, color 160ms ease, border-color 160ms ease, transform 160ms ease;
      }

      .mega-list a:hover {
        text-decoration: none;
        color: var(--text);
        background: rgba(241, 245, 249, 0.86);
        border-color: rgba(var(--accent-tech-rgb), 0.22);
        transform: translateX(1px);
      }

      .mega-menu-note {
        margin-top: var(--space-sm);
        padding: 11px 12px;
        border-radius: 10px;
        border: 1px solid rgba(var(--accent-tech-rgb), 0.24);
        background:
          linear-gradient(90deg, rgba(var(--accent-tech-rgb), 0.08), rgba(248, 250, 252, 0.92) 32%),
          rgba(248, 250, 252, 0.92);
        font-size: 13px;
        line-height: 1.5;
        color: rgba(15, 23, 42, 0.9);
      }

      .mega-menu-note a {
        font-weight: 800;
        color: var(--accent-tech-dark);
      }

      .mega-group-heading:focus-visible,
      .mega-list a:focus-visible,
      .mega-menu-all:focus-visible {
        outline: 3px solid var(--ring);
        outline-offset: 2px;
      }

      @media (prefers-reduced-motion: reduce) {
        .nav-chevron {
          transition: none;
          transform: translateY(0.5px) rotate(0deg);
        }

        .mega-menu,
        .nav-item--mega:hover .mega-menu,
        .nav-item--mega:focus-within .mega-menu {
          transition: none;
          transform: translate(-50%, 0) scale(1);
        }

        .btn {
          transition: none;
        }
      }

      .header-actions {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        margin-left: auto;
      }

      .language-switch {
        display: inline-flex;
        align-items: center;
        margin-left: 14px;
        padding-left: 16px;
        border-left: 1px solid rgba(148, 163, 184, 0.32);
        text-decoration: none;
        white-space: nowrap;
      }

      .language-switch:hover {
        text-decoration: none;
      }

      .language-switch:focus-visible {
        outline: 3px solid var(--ring);
        outline-offset: 4px;
        border-radius: 12px;
      }

      .language-switch-flag {
        width: 22px;
        height: 22px;
        flex: 0 0 22px;
        transition:
          transform 180ms ease,
          filter 180ms ease;
      }

      .language-switch-flag img,
      .language-switch-flag svg {
        width: 100%;
        height: 100%;
        display: block;
        object-fit: contain;
      }

      .language-switch:hover .language-switch-flag {
        transform: translateY(-0.5px);
        filter: saturate(1.05);
      }

      .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
        min-height: 44px;
        padding: 0 24px;
        border-radius: var(--btn-radius);
        border: 2px solid transparent;
        font-size: 15px;
        font-weight: 700;
        letter-spacing: -0.02em;
        white-space: nowrap;
        text-decoration: none;
        transition:
          background 160ms ease,
          border-color 160ms ease,
          color 160ms ease,
          box-shadow 180ms ease,
          transform 180ms ease;
      }

      .btn:hover {
        text-decoration: none;
      }

      .btn:focus-visible {
        outline: 3px solid var(--ring);
        outline-offset: 2px;
      }

      .btn-primary {
        background: var(--brand-blue);
        color: #ffffff;
        border-color: var(--brand-blue);
        box-shadow: none;
      }

      .btn-primary:hover {
        background: var(--brand-blue-deep);
        border-color: var(--brand-blue-deep);
        transform: translateY(-0.5px);
        box-shadow: none;
      }

      .btn-ghost {
        border-color: var(--brand-blue);
        background: rgba(255, 255, 255, 0.96);
        color: var(--brand-blue);
      }

      .btn-ghost:hover {
        background: rgba(0, 80, 157, 0.04);
        border-color: var(--brand-blue-deep);
        color: var(--brand-blue-deep);
        transform: translateY(-0.5px);
      }

      .btn:active {
        transform: translateY(0);
      }

      .mobile-nav {
        display: none;
      }

      .mobile-nav summary {
        list-style: none;
      }

      .mobile-nav summary::-webkit-details-marker {
        display: none;
      }

      .mobile-nav-trigger {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        padding: 10px 14px;
        border-radius: var(--radius-full);
        border: 1px solid var(--border);
        background: rgba(255, 255, 255, 0.75);
        color: var(--text);
        font-size: 13px;
        font-weight: 700;
        cursor: pointer;
      }

      .mobile-nav-icon {
        display: inline-flex;
        width: 18px;
        height: 18px;
        color: currentColor;
      }

      .mobile-nav-icon svg {
        width: 18px;
        height: 18px;
        display: block;
      }

      .mobile-nav-panel {
        position: absolute;
        right: 0;
        top: calc(var(--header-height) - 10px);
        width: min(360px, calc(100vw - 40px));
        padding: 10px;
        border-radius: var(--radius);
        border: 1px solid var(--border);
        background: var(--surface);
        box-shadow: var(--shadow);
      }

      .mobile-nav-panel a {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
        padding: 12px 12px;
        border-radius: var(--radius-sm);
        color: var(--text);
        font-weight: 650;
        font-size: 13px;
      }

      .mobile-nav-panel a:hover {
        background: rgba(226, 232, 240, 0.55);
        text-decoration: none;
      }

      .mobile-nav-panel .mobile-nav-cta {
        margin-top: 8px;
        background: rgba(var(--accent-tech-rgb), 0.12);
        color: var(--accent-tech-dark);
      }

      main {
        flex: 1;
      }

      .section {
        padding: var(--space-section) 0;
      }

      .is-lead-plan .prose > .content-page-cta:first-child {
        margin-top: 0;
      }

      .is-lead-plan .prose,
      .is-lead-plan.is-content .prose {
        max-width: 100%;
      }

      .is-lead-plan .prose h2 {
        margin-top: clamp(34px, 5vw, 52px);
      }

      .is-lead-plan .leadplan-hero {
        margin: 0 0 clamp(26px, 4vw, 42px);
        display: grid;
        grid-template-columns: minmax(0, 1fr) minmax(0, 1.1fr);
        gap: var(--space-md);
        align-items: start;
      }

      .is-lead-plan .leadplan-kicker {
        margin: 0 0 8px;
        font-size: 12px;
        font-weight: 800;
        letter-spacing: 0.06em;
        text-transform: uppercase;
        color: var(--accent-tech-dark);
      }

      .is-lead-plan .leadplan-title {
        margin: 0 0 10px;
        color: var(--text);
        font-size: clamp(26px, 3.2vw, 38px);
        line-height: 1.08;
        letter-spacing: -0.035em;
      }

      .is-lead-plan .leadplan-subtitle {
        margin: 0 0 12px;
        color: rgba(30, 41, 59, 0.86);
        max-width: 58ch;
      }

      .is-lead-plan .leadplan-reassure {
        margin: 0;
        font-size: 13px;
        font-weight: 650;
        color: rgba(51, 65, 85, 0.95);
      }

      .is-lead-plan .leadplan-points {
        list-style: none;
        margin: 14px 0 0;
        padding: 0;
        display: grid;
        gap: 7px;
      }

      .is-lead-plan .leadplan-points li {
        margin: 0;
        padding: 0;
        font-size: 14px;
        color: rgba(30, 41, 59, 0.92);
      }

      .is-lead-plan .leadplan-points li::before {
        content: none;
      }

      .is-lead-plan .leadplan-hero-image {
        margin: 16px 0 0;
      }

      .is-lead-plan .leadplan-hero-image img {
        width: 100%;
        max-width: 512px;
        height: 600px;
        display: block;
        border-radius: 14px;
        border: 0;
        box-shadow: 0 14px 28px rgba(2, 6, 23, 0.12);
        opacity: 0.88;
        object-fit: cover;
        object-position: center top;
      }

      .is-lead-plan .leadplan-form-card {
        margin: 0;
        padding: clamp(16px, 2.2vw, 22px);
        border-radius: 14px;
        border: 1px solid rgba(var(--accent-tech-rgb), 0.32);
        background:
          radial-gradient(circle at 96% 4%, rgba(var(--accent-tech-rgb), 0.14), transparent 52%),
          linear-gradient(165deg, rgba(var(--accent-tech-rgb), 0.08), rgba(255, 255, 255, 0.97) 60%),
          rgba(255, 255, 255, 0.97);
        box-shadow: 0 16px 30px rgba(2, 6, 23, 0.07);
      }

      .is-lead-plan .leadplan-form-card h2 {
        margin: 0 0 6px;
        padding-top: 0;
        border-top: 0;
        font-size: 21px;
      }

      .is-lead-plan .leadplan-form-card p {
        margin: 0 0 10px;
        max-width: 52ch;
      }

      .is-lead-plan .leadplan-form-card .hubspot-form-wrap {
        margin-top: 10px;
      }

      .is-lead-plan .leadplan-flow {
        display: grid;
        grid-template-columns: repeat(5, minmax(0, 1fr));
        gap: 10px;
        margin: var(--space-sm) 0 0;
      }

      .is-lead-plan .leadplan-step {
        margin: 0;
        padding: 12px 12px;
        border-radius: 12px;
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(255, 255, 255, 0.96);
        box-shadow: 0 8px 20px rgba(2, 6, 23, 0.05);
      }

      .is-lead-plan .leadplan-step-index {
        margin: 0 0 8px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 24px;
        height: 24px;
        border-radius: 999px;
        background: rgba(var(--accent-tech-rgb), 0.16);
        color: var(--accent-tech-dark);
        font-size: 12px;
        font-weight: 780;
      }

      .is-lead-plan .leadplan-step-title {
        margin: 0;
        color: var(--text);
        font-size: 14px;
        line-height: 1.45;
      }

      .is-lead-plan .leadplan-final-cta {
        margin-top: clamp(36px, 5vw, 56px);
      }

      .section.compact {
        padding: var(--space-section-compact) 0;
      }

      .hero {
        padding: clamp(44px, 6vw, 56px) 0 var(--space-lg);
        background: radial-gradient(circle at top left, rgba(var(--accent-tech-rgb), 0.16), transparent 45%),
          radial-gradient(circle at top right, rgba(var(--primary-rgb), 0.08), transparent 55%);
        border-bottom: 1px solid rgba(226, 232, 240, 0.9);
      }

      .hero-grid {
        display: grid;
        grid-template-columns: minmax(0, 1fr);
        gap: 26px;
        align-items: start;
      }

      .hero-content {
        max-width: 76ch;
      }

      .hero-eyebrow {
        margin: 0 0 var(--space-2xs);
        font-size: 13px;
        font-weight: 750;
        letter-spacing: -0.01em;
        color: var(--accent-tech-dark);
      }

      .hero h1 {
        margin: 0 0 var(--space-xs);
        font-size: clamp(34px, 4vw, 48px);
        line-height: 1.05;
        letter-spacing: -0.045em;
      }

      .hero h1 span {
        color: var(--accent-tech-dark);
      }

      .hero-lead {
        margin: 0 0 var(--space-sm);
        max-width: 64ch;
        color: var(--muted);
        font-size: 16px;
        line-height: 1.7;
      }

      .hero-cta {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: 10px;
        margin-top: var(--space-sm);
      }

      .hero-note {
        margin-top: var(--space-xs);
        color: var(--muted-soft);
        font-size: 12px;
      }

      .section-header {
        display: grid;
        gap: var(--space-2xs);
        margin-bottom: 18px;
      }

      .section-title {
        margin: 0;
        font-size: 22px;
        letter-spacing: -0.02em;
      }

      .section-lead {
        margin: 0;
        color: var(--muted);
        max-width: 56ch;
        font-size: 13px;
        line-height: 1.55;
      }

      .services-grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 16px;
        margin-top: 18px;
      }

      .feature-stack {
        display: grid;
        gap: 40px;
        margin-top: 24px;
      }

      .feature-panel {
        display: grid;
        grid-template-columns: minmax(0, 1fr);
        gap: 32px;
        padding: 44px;
        border-radius: 12px;
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(255, 255, 255, 0.98);
      }
      .feature-panel img {
        width: 100%;
        height: auto;
        display: block;
        border-radius: 10px;
        border: 1px solid;
        border-color: rgb(213, 220, 226) rgb(213, 220, 226) rgb(184, 194, 204);
      }

      .feature-panel-copy {
        max-width: 36ch;
      }

      .feature-eyebrow {
        margin: 0 0 8px;
        font-size: 13px;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        color: var(--text);
      }

      .feature-title {
        margin: 0 0 14px;
        font-size: clamp(32px, 3.6vw, 48px);
        line-height: 1;
        letter-spacing: -0.03em;
        color: var(--text);
      }

      .feature-desc {
        margin: 0;
        font-size: 18px;
        line-height: 1.42;
        color: var(--muted);
      }

      .service-card {
        display: flex;
        flex-direction: column;
        gap: 10px;
        padding: 20px;
        border-radius: 8px;
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(255, 255, 255, 0.9);
        box-shadow: none;
        transition: transform 140ms ease, box-shadow 140ms ease, border-color 140ms ease;
        color: inherit;
      }

      .service-card:hover {
        text-decoration: none;
        transform: translateY(-1px);
        border-color: rgba(var(--primary-rgb), 0.28);
        box-shadow: none;
      }

      .service-title {
        margin: 0;
        font-size: 14px;
        font-weight: 800;
        letter-spacing: -0.01em;
        color: var(--text);
      }

      .service-desc {
        margin: 0;
        color: var(--muted);
        font-size: 13px;
        line-height: 1.5;
      }

      .service-meta {
        margin-top: auto;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        color: var(--accent-tech-dark);
        font-weight: 750;
        font-size: 13px;
      }

      .service-meta span {
        font-size: 16px;
      }

      .pricing-grid {
        display: grid;
        grid-template-columns: minmax(0, 1fr);
        gap: 20px;
      }

      .pricing-card {
        display: flex;
        flex-direction: column;
        gap: 18px;
        padding: 28px;
        border-radius: 12px;
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(255, 255, 255, 0.98);
      }

      .pricing-card--featured {
        border-color: rgba(var(--highlight-rgb), 0.72);
        box-shadow: 0 18px 40px rgba(2, 6, 23, 0.08);
        background:
          radial-gradient(circle at top right, rgba(var(--highlight-rgb), 0.18), transparent 34%),
          linear-gradient(180deg, rgba(255, 255, 255, 0.99) 0%, rgba(248, 250, 252, 0.99) 100%);
      }

      .pricing-top {
        display: flex;
        flex-direction: column;
        gap: 10px;
      }

      .pricing-kicker {
        margin: 0;
        font-size: 12px;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        color: var(--text);
      }

      .pricing-title {
        margin: 0;
        font-size: 28px;
        line-height: 1;
        letter-spacing: -0.04em;
        color: var(--text);
      }

      .pricing-price {
        margin: 0;
        font-size: clamp(30px, 3vw, 42px);
        line-height: 1;
        letter-spacing: -0.05em;
        color: var(--text);
      }

      .pricing-period {
        font-size: 15px;
        color: var(--muted);
        font-weight: 600;
      }

      .pricing-note {
        margin: 0;
        color: var(--muted);
        font-size: 14px;
        line-height: 1.6;
      }

      .pricing-list {
        margin: 0;
        padding: 0;
        list-style: none;
        display: grid;
        gap: 10px;
        color: var(--text);
      }

      .pricing-list li {
        position: relative;
        padding-left: 18px;
        line-height: 1.55;
      }

      .pricing-list li::before {
        content: "";
        position: absolute;
        left: 0;
        top: 0.7em;
        width: 7px;
        height: 7px;
        border-radius: 999px;
        background: rgba(var(--highlight-rgb), 0.95);
        transform: translateY(-50%);
      }

      .pricing-actions {
        margin-top: auto;
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
      }

      .page-header {
        padding: 0;
        margin: 0;
      }

      .page-header h1 {
        margin: 0 0 var(--space-2xs);
        font-size: clamp(28px, 2.6vw, 44px);
        line-height: 1.06;
        letter-spacing: -0.05em;
      }

      .page-lead {
        margin: 0;
        color: rgba(15, 23, 42, 0.76);
        max-width: 72ch;
        font-size: 15px;
        line-height: 1.8;
      }

      .page-hero {
        position: relative;
        padding: var(--space-section-compact) 0 var(--space-lg);
        background: linear-gradient(180deg, rgba(255, 255, 255, 0.9) 0%, rgba(248, 250, 252, 0.9) 100%);
        border-bottom: 1px solid rgba(226, 232, 240, 0.9);
        overflow: hidden;
      }

      .page-hero::before {
        content: "";
        position: absolute;
        inset: -40% -20% auto -20%;
        height: 380px;
        background:
          radial-gradient(circle at 18% 28%, rgba(var(--accent-tech-rgb), 0.16), transparent 60%),
          radial-gradient(circle at 62% 52%, rgba(2, 6, 23, 0.08), transparent 58%),
          radial-gradient(circle at 84% 18%, rgba(var(--accent-tech-rgb), 0.10), transparent 52%);
        pointer-events: none;
      }

      .page-hero-inner {
        position: relative;
        display: flex;
        flex-direction: column;
        gap: var(--space-2xs);
      }

      .page-breadcrumbs {
        position: relative;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: 8px;
        margin: 0 0 var(--space-sm);
        color: rgba(71, 85, 105, 0.85);
        font-size: 12px;
        font-weight: 700;
      }

      .page-breadcrumbs a {
        color: rgba(71, 85, 105, 0.95);
      }

      .page-breadcrumbs .crumb-sep {
        color: rgba(148, 163, 184, 0.9);
      }

      

      .prose {
        max-width: 76ch;
        color: var(--muted);
        font-size: 15px;
        line-height: 1.8;
      }

      .prose h2,
      .prose h3 {
        color: var(--text);
        letter-spacing: -0.015em;
      }

      .prose h2 {
        margin: var(--space-md) 0 var(--space-2xs);
        font-size: 18px;
      }

      .prose h3 {
        margin: var(--space-sm) 0 var(--space-2xs);
        font-size: 16px;
      }

      .prose p {
        margin: 0 0 var(--space-xs);
      }

      .prose figure {
        margin: var(--space-sm) 0;
      }

      .prose img {
        max-width: 100%;
        height: auto;
        display: block;
        border-radius: var(--radius);
        border: 1px solid rgba(226, 232, 240, 0.95);
        box-shadow: 0 18px 40px rgba(2, 6, 23, 0.08);
        background: rgba(255, 255, 255, 0.9);
      }

      .prose figcaption {
        margin-top: 10px;
        color: var(--muted-soft);
        font-size: 12px;
        line-height: 1.6;
      }

      .prose .visual-break {
        margin: var(--space-md) 0;
      }

      .prose .visual-break img {
        width: 100%;
        max-height: 420px;
        object-fit: cover;
      }

      .prose .visual-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: var(--space-sm);
        margin: var(--space-md) 0;
      }

      .prose .visual-grid figure {
        margin: 0;
      }

      .is-content .prose {
        max-width: 90ch;
      }

      .is-content .prose > p,
      .is-content .prose > ul,
      .is-content .prose > ol,
      .is-content .prose > blockquote {
        max-width: 68ch;
      }

      .is-content .prose > p {
        margin-bottom: var(--space-sm);
      }

      .is-content .prose h2 {
        margin-top: clamp(42px, 6vw, 64px);
        margin-bottom: var(--space-xs);
        padding-top: 14px;
        border-top: 1px solid rgba(226, 232, 240, 0.9);
      }

      .is-content .prose h2:first-of-type {
        margin-top: var(--space-sm);
        padding-top: 0;
        border-top: 0;
      }

      .is-content .prose h3 {
        margin-top: var(--space-md);
      }

      .is-content .prose .content-page-cta {
        margin-top: clamp(44px, 6vw, 68px);
      }

      .about-split {
        display: grid;
        grid-template-columns: minmax(0, 1fr);
        gap: var(--space-sm);
        align-items: start;
        margin: 0 0 var(--space-sm);
      }

      .about-photo {
        margin: 0;
      }

      .about-intro p {
        margin: 0 0 var(--space-xs);
      }

      .prose ul {
        margin: 0 0 var(--space-sm);
        padding-left: 18px;
      }

      .prose li {
        margin-bottom: 6px;
      }

      .prose pre {
        margin: var(--space-sm) 0;
        padding: var(--space-xs) var(--space-xs);
        border-radius: var(--radius);
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(248, 250, 252, 0.9);
        overflow: auto;
        box-shadow: 0 10px 26px rgba(2, 6, 23, 0.06);
        font-size: 13px;
        line-height: 1.55;
        color: var(--text);
      }

      .prose code {
        font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
        font-size: 0.95em;
      }

      .prose .trust-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: var(--space-sm);
        margin: var(--space-sm) 0;
      }

      .prose .trust-card {
        padding: 18px;
        border-radius: 8px;
        border: 1px solid rgba(226, 232, 240, 0.9);
        background: rgba(255, 255, 255, 0.92);
        box-shadow: 0 8px 22px rgba(2, 6, 23, 0.05);
      }

      .prose .trust-card h3 {
        margin-top: 0;
      }

      .prose .step-flow {
        list-style: none;
        margin: var(--space-sm) 0;
        padding: 0;
        display: grid;
        gap: 10px;
      }

      .prose .step-flow li {
        margin: 0;
        padding: 12px 14px;
        border-radius: 10px;
        border: 1px solid rgba(226, 232, 240, 0.9);
        background: rgba(248, 250, 252, 0.86);
      }

      .prose .step-flow li::before {
        display: none;
      }

      .prose .highlight-note {
        margin: var(--space-sm) 0;
        padding: 12px 14px;
        border-radius: 10px;
        border: 1px solid rgba(var(--accent-tech-rgb), 0.22);
        background: linear-gradient(95deg, rgba(var(--accent-tech-rgb), 0.07), rgba(248, 250, 252, 0.94));
        color: rgba(15, 23, 42, 0.92);
      }

      .prose .content-page-cta {
        margin-top: var(--space-md);
        padding: var(--space-sm);
        border-radius: var(--radius-sm);
        border: 1px solid rgba(var(--accent-tech-rgb), 0.28);
        background:
          radial-gradient(circle at 86% 10%, rgba(var(--accent-tech-rgb), 0.14), transparent 46%),
          linear-gradient(160deg, rgba(var(--accent-tech-rgb), 0.1), rgba(255, 255, 255, 0.94) 56%),
          rgba(255, 255, 255, 0.94);
        box-shadow:
          0 12px 28px rgba(2, 6, 23, 0.08),
          0 1px 0 rgba(255, 255, 255, 0.75) inset;
      }

      .prose .content-page-cta h2 {
        margin: 0 0 var(--space-2xs);
        font-size: 19px;
        letter-spacing: -0.02em;
      }

      .prose .content-page-cta p {
        margin: 0 0 var(--space-sm);
        max-width: 64ch;
      }

      .table-scroll {
        margin: var(--space-sm) 0;
        overflow-x: auto;
        border-radius: 12px;
        border: 1px solid rgba(226, 232, 240, 0.92);
        background: rgba(255, 255, 255, 0.96);
        box-shadow: 0 10px 24px rgba(2, 6, 23, 0.05);
      }

      .usecase-table {
        width: 100%;
        border-collapse: collapse;
        font-size: 14px;
        line-height: 1.5;
        min-width: 640px;
      }

      .usecase-table th,
      .usecase-table td {
        padding: 11px 12px;
        border-bottom: 1px solid rgba(226, 232, 240, 0.86);
        text-align: left;
        vertical-align: top;
      }

      .usecase-table th {
        background: rgba(248, 250, 252, 0.9);
        color: var(--text);
        font-size: 12px;
        font-weight: 780;
        letter-spacing: 0.03em;
        text-transform: uppercase;
      }

      .usecase-table td {
        color: var(--muted);
      }

      .usecase-table tr:last-child td {
        border-bottom: 0;
      }

      .usecase-table tbody tr:nth-child(even) {
        background: rgba(248, 250, 252, 0.62);
      }

      .usecase-table td strong {
        color: var(--text);
      }

      .compare-table th {
        background:
          linear-gradient(165deg, rgba(var(--accent-tech-rgb), 0.12), rgba(248, 250, 252, 0.95) 62%),
          rgba(248, 250, 252, 0.95);
      }

      .compare-table td:nth-child(2),
      .compare-table td:nth-child(3),
      .compare-table td:nth-child(4) {
        font-variant-numeric: tabular-nums;
      }

      .compare-table th:nth-child(4),
      .compare-table td:nth-child(4) {
        background: rgba(var(--accent-tech-rgb), 0.12);
        color: var(--text);
        font-weight: 700;
      }

      .prose .usecase-kpi-grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 10px;
        margin: var(--space-sm) 0;
      }

      .prose .usecase-kpi-card {
        padding: 14px;
        border-radius: 12px;
        border: 1px solid rgba(226, 232, 240, 0.92);
        background:
          linear-gradient(165deg, rgba(var(--accent-tech-rgb), 0.06), rgba(255, 255, 255, 0.96) 58%),
          rgba(255, 255, 255, 0.96);
      }

      .prose .usecase-kpi-label {
        margin: 0 0 4px;
        font-size: 11px;
        font-weight: 780;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        color: var(--accent-tech-dark);
      }

      .prose .usecase-kpi-value {
        margin: 0;
        font-size: 24px;
        line-height: 1.1;
        letter-spacing: -0.03em;
        color: var(--text);
      }

      .prose .usecase-kpi-note {
        margin: 6px 0 0;
        font-size: 13px;
        color: var(--muted);
      }

      .conversion-compare {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: var(--space-sm);
        margin: var(--space-sm) 0;
      }

      .compare-card {
        padding: var(--space-sm);
        border-radius: var(--radius-sm);
        border: 1px solid rgba(226, 232, 240, 0.94);
        background: rgba(255, 255, 255, 0.94);
        box-shadow: 0 10px 24px rgba(2, 6, 23, 0.05);
      }

      .compare-card--after {
        border-color: rgba(var(--accent-tech-rgb), 0.32);
        background:
          linear-gradient(165deg, rgba(var(--accent-tech-rgb), 0.1), rgba(255, 255, 255, 0.96) 58%),
          rgba(255, 255, 255, 0.96);
      }

      .compare-label {
        margin: 0 0 6px;
        font-size: 11px;
        font-weight: 780;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        color: var(--accent-tech-dark);
      }

      .compare-title {
        margin: 0 0 8px;
        font-size: 19px;
        letter-spacing: -0.02em;
      }

      .compare-points {
        margin: 0;
        padding-left: 18px;
      }

      .compare-points li {
        margin: 0 0 6px;
      }

      .compare-result {
        margin: 10px 0 0;
        font-size: 13px;
        font-weight: 650;
        color: rgba(15, 23, 42, 0.92);
      }

      .example-scenario {
        margin: var(--space-sm) 0;
        padding: var(--space-sm);
        border-radius: var(--radius-sm);
        border: 1px solid rgba(var(--accent-tech-rgb), 0.3);
        background:
          radial-gradient(circle at 100% 4%, rgba(var(--accent-tech-rgb), 0.14), transparent 50%),
          linear-gradient(165deg, rgba(var(--accent-tech-rgb), 0.08), rgba(255, 255, 255, 0.95) 60%),
          rgba(255, 255, 255, 0.95);
      }

      .example-scenario p {
        margin: 0 0 8px;
      }

      .example-scenario p:last-child {
        margin-bottom: 0;
      }

      .lead-plan-panel {
        margin: var(--space-sm) 0;
        padding: var(--space-sm);
        border-radius: var(--radius-sm);
        border: 1px solid rgba(var(--accent-tech-rgb), 0.3);
        background:
          radial-gradient(circle at 96% 4%, rgba(var(--accent-tech-rgb), 0.15), transparent 52%),
          linear-gradient(165deg, rgba(var(--accent-tech-rgb), 0.08), rgba(255, 255, 255, 0.96) 60%),
          rgba(255, 255, 255, 0.96);
      }

      .lead-plan-list {
        margin: 10px 0 0;
        padding-left: 18px;
      }

      .lead-plan-list li {
        margin-bottom: 6px;
      }

      .lead-plan-form {
        margin-top: var(--space-sm);
        display: grid;
        gap: 10px;
      }

      .lead-plan-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 10px;
      }

      @media (max-width: 760px) {
        .lead-plan-grid {
          grid-template-columns: 1fr;
        }
      }

      .lead-plan-field {
        display: grid;
        gap: 6px;
      }

      .lead-plan-field label {
        font-size: 12px;
        font-weight: 650;
        color: var(--text);
      }

      .lead-plan-field input,
      .lead-plan-field select,
      .lead-plan-field textarea {
        width: 100%;
        min-height: 42px;
        border-radius: 10px;
        border: 1px solid rgba(148, 163, 184, 0.5);
        background: rgba(255, 255, 255, 0.96);
        color: var(--text);
        font: inherit;
        padding: 9px 11px;
        box-sizing: border-box;
      }

      .lead-plan-field textarea {
        min-height: 104px;
        resize: vertical;
      }

      .lead-plan-field input:focus,
      .lead-plan-field select:focus,
      .lead-plan-field textarea:focus {
        outline: 3px solid var(--ring);
        border-color: rgba(var(--accent-tech-rgb), 0.5);
      }

      .prose .hubspot-form-wrap {
        margin-top: var(--space-xs);
        padding: var(--space-sm);
        border-radius: var(--radius-sm);
        border: 1px solid rgba(226, 232, 240, 0.92);
        background: rgba(255, 255, 255, 0.94);
      }

      .prose .hubspot-form-wrap .hs-form-field {
        margin-bottom: 12px;
      }

      .prose .hubspot-form-wrap .hs-form-field > label {
        margin-bottom: 6px;
        display: inline-block;
        color: var(--text);
        font-weight: 650;
        font-size: 12px;
      }

      .prose .hubspot-form-wrap .hs-input,
      .prose .hubspot-form-wrap textarea,
      .prose .hubspot-form-wrap select {
        width: 100% !important;
        min-height: 42px;
        border-radius: 10px;
        border: 1px solid rgba(148, 163, 184, 0.5);
        background: rgba(255, 255, 255, 0.96);
        color: var(--text);
        font: inherit;
        padding: 9px 11px;
        box-sizing: border-box;
      }

      .prose .hubspot-form-wrap textarea {
        min-height: 108px;
      }

      .prose .hubspot-form-wrap .hs-input:focus,
      .prose .hubspot-form-wrap textarea:focus,
      .prose .hubspot-form-wrap select:focus {
        outline: 3px solid var(--ring);
        outline-offset: 0;
        border-color: rgba(var(--accent-tech-rgb), 0.5);
      }

      .prose .hubspot-form-wrap .hs-button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-height: var(--btn-height);
        padding: 10px 15px;
        border-radius: var(--btn-radius);
        border: 1px solid rgba(var(--primary-rgb), 0.08);
        background: linear-gradient(180deg, var(--primary) 0%, var(--primary-soft) 100%);
        color: #04110a;
        font-size: 13px;
        font-weight: 760;
        letter-spacing: -0.008em;
        box-shadow:
          0 0 0 1px rgba(255, 255, 255, 0.22) inset,
          0 0 18px rgba(var(--primary-rgb), 0.24),
          0 4px 10px rgba(var(--primary-rgb), 0.15),
          0 1px 2px rgba(2, 6, 23, 0.06);
        cursor: pointer;
      }

      .prose .hubspot-form-wrap .hs-button:hover {
        filter: brightness(1.015);
      }

      .is-contact .prose {
        max-width: 100%;
      }

      .is-contact .prose .contact-layout {
        display: grid;
        grid-template-columns: minmax(0, 1fr);
        gap: var(--space-sm);
        margin: 0 0 var(--space-sm);
      }

      .is-contact .prose .contact-intro,
      .is-contact .prose .contact-form-card {
        padding: var(--space-sm);
        border-radius: var(--radius-sm);
        border: 1px solid rgba(226, 232, 240, 0.92);
        background: rgba(255, 255, 255, 0.94);
        box-shadow: 0 10px 24px rgba(2, 6, 23, 0.05);
      }

      .is-contact .prose .contact-intro {
        background:
          radial-gradient(circle at 82% 14%, rgba(var(--accent-tech-rgb), 0.12), transparent 44%),
          linear-gradient(150deg, rgba(var(--accent-tech-rgb), 0.08), rgba(255, 255, 255, 0.95) 56%),
          rgba(255, 255, 255, 0.95);
      }

      .is-contact .prose .contact-kicker {
        margin: 0 0 7px;
        font-size: 12px;
        font-weight: 800;
        letter-spacing: 0.06em;
        text-transform: uppercase;
        color: var(--accent-tech-dark);
      }

      .is-contact .prose .contact-intro h2,
      .is-contact .prose .contact-form-card h2 {
        margin-top: 0;
      }

      .is-contact .prose .contact-photo {
        margin: 0 0 var(--space-xs);
      }

      .is-contact .prose .contact-photo img {
        width: 100%;
        aspect-ratio: 1 / 1;
        object-fit: cover;
        object-position: center top;
        border-radius: 12px;
      }

      .is-contact .prose .contact-points {
        margin: var(--space-xs) 0 0;
        padding-left: 18px;
      }

      .is-contact .prose .contact-points li {
        margin-bottom: 8px;
      }

      .is-contact .prose .contact-steps {
        list-style: none;
        margin: var(--space-xs) 0 0;
        padding: 0;
        display: grid;
        gap: 8px;
      }

      .is-contact .prose .contact-steps li {
        margin: 0;
        padding: 10px 12px;
        border-radius: 10px;
        border: 1px solid rgba(226, 232, 240, 0.92);
        background: rgba(248, 250, 252, 0.88);
      }

      .is-contact .prose .contact-steps li::before {
        display: none;
      }

      .is-why-us .page-hero::before {
        background:
          radial-gradient(circle at 14% 22%, rgba(var(--accent-tech-rgb), 0.18), transparent 54%),
          radial-gradient(circle at 82% 18%, rgba(var(--primary-rgb), 0.14), transparent 56%);
      }

      .is-why-us .prose {
        max-width: 96ch;
      }

      .is-why-us .prose .whyus-intro {
        display: grid;
        grid-template-columns: minmax(0, 1.25fr) minmax(0, 0.95fr);
        gap: var(--space-sm);
        margin: var(--space-sm) 0;
      }

      .is-why-us .prose .whyus-intro-main,
      .is-why-us .prose .whyus-intro-side {
        padding: var(--space-sm);
        border-radius: var(--radius-sm);
        border: 1px solid rgba(226, 232, 240, 0.92);
        box-shadow: 0 10px 24px rgba(2, 6, 23, 0.05);
      }

      .is-why-us .prose .whyus-intro-main {
        background:
          radial-gradient(circle at 82% 12%, rgba(var(--accent-tech-rgb), 0.13), transparent 44%),
          linear-gradient(158deg, rgba(var(--accent-tech-rgb), 0.08), rgba(255, 255, 255, 0.95) 55%),
          rgba(255, 255, 255, 0.95);
      }

      .is-why-us .prose .whyus-intro-side {
        background: rgba(255, 255, 255, 0.96);
      }

      .is-why-us .prose .whyus-kicker {
        margin: 0 0 8px;
        font-size: 12px;
        font-weight: 800;
        letter-spacing: 0.06em;
        text-transform: uppercase;
        color: var(--accent-tech-dark);
      }

      .is-why-us .prose .whyus-photo {
        margin: 0 0 var(--space-xs);
      }

      .is-why-us .prose .whyus-photo img {
        width: 100%;
        height: auto;
        border-radius: 12px;
      }

      .is-why-us .prose .whyus-intro h2 {
        margin-top: 0;
      }

      .is-why-us .prose .whyus-proof {
        margin: 0;
        padding: 0;
        list-style: none;
        display: grid;
        gap: 8px;
      }

      .is-why-us .prose .whyus-proof li {
        margin: 0;
        padding: 10px 12px;
        border-radius: 10px;
        border: 1px solid rgba(226, 232, 240, 0.9);
        background: rgba(248, 250, 252, 0.88);
      }

      .is-why-us .prose .whyus-proof li::before {
        display: none;
      }

      .is-why-us .prose .whyus-pillars {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 10px;
        margin: var(--space-sm) 0;
      }

      .is-why-us .prose .whyus-pillar {
        padding: 14px;
        border-radius: 12px;
        border: 1px solid rgba(226, 232, 240, 0.9);
        background: rgba(255, 255, 255, 0.95);
      }

      .is-why-us .prose .whyus-pillar h3 {
        margin-top: 0;
      }

      .is-why-us .prose .whyus-quote {
        margin: var(--space-sm) 0;
        padding: 14px 16px;
        border-left: 3px solid rgba(var(--accent-tech-rgb), 0.5);
        border-radius: 0 10px 10px 0;
        background: rgba(248, 250, 252, 0.9);
        color: var(--text);
        font-size: 15px;
        line-height: 1.7;
      }

      .is-services .page-hero {
        padding: clamp(28px, 3.8vw, 44px) 0 clamp(24px, 3.2vw, 34px);
        background: linear-gradient(180deg, rgba(255, 255, 255, 0.98) 0%, rgba(248, 250, 252, 0.95) 100%);
      }

      .is-services .page-hero::before {
        inset: 0;
        height: auto;
        background:
          radial-gradient(circle at 14% 24%, rgba(148, 163, 184, 0.12), transparent 52%),
          radial-gradient(circle at 78% 28%, rgba(148, 163, 184, 0.08), transparent 50%);
      }

      .is-services .page-breadcrumbs {
        margin-bottom: var(--space-xs);
        font-weight: 650;
      }

      .is-services .page-header h1 {
        font-size: clamp(30px, 3vw, 44px);
        line-height: 1.08;
        letter-spacing: -0.035em;
      }

      .is-services .page-lead {
        max-width: 68ch;
        font-size: 16px;
        line-height: 1.72;
        color: rgba(15, 23, 42, 0.78);
      }

      .is-services .prose {
        max-width: 82ch;
        font-size: 16px;
        line-height: 1.78;
        color: rgba(51, 65, 85, 0.98);
      }

      .is-services .prose h2 {
        margin: calc(var(--space-lg) + 2px) 0 var(--space-xs);
        font-size: clamp(21px, 2vw, 25px);
        line-height: 1.24;
        letter-spacing: -0.022em;
      }

      .is-services .prose h3 {
        margin: var(--space-md) 0 10px;
        font-size: 18px;
        line-height: 1.32;
      }

      .is-services .prose p {
        margin-bottom: 14px;
      }

      .is-services .prose ul {
        margin: 0 0 var(--space-md);
        padding-left: 0;
        list-style: none;
        display: grid;
        gap: 8px;
      }

      .is-services .prose ol {
        margin: 0 0 var(--space-md);
        padding-left: 22px;
        display: grid;
        gap: 8px;
      }

      .is-services .prose li {
        margin: 0;
        position: relative;
        padding-left: 16px;
        line-height: 1.58;
      }

      .is-services .prose li::before {
        content: "";
        position: absolute;
        left: 0;
        top: 0.67em;
        width: 6px;
        height: 6px;
        border-radius: 999px;
        background: rgba(100, 116, 139, 0.56);
      }

      .is-services .prose ol li {
        padding-left: 0;
      }

      .is-services .prose ol li::before {
        content: none;
      }

      .is-services .prose a {
        color: var(--text);
        text-decoration-color: rgba(100, 116, 139, 0.62);
        text-decoration-thickness: 1px;
        text-underline-offset: 3px;
      }

      .is-services .prose a:hover {
        color: var(--text);
        text-decoration-color: rgba(15, 23, 42, 0.88);
      }

      .is-services .prose .faq-accordion {
        display: grid;
        gap: var(--space-xs);
        margin: 0 0 var(--space-lg);
      }

      .is-services .prose .faq-item {
        border: 1px solid rgba(203, 213, 225, 0.95);
        border-radius: 12px;
        background: rgba(255, 255, 255, 0.98);
        overflow: hidden;
        box-shadow: 0 2px 8px rgba(2, 6, 23, 0.03);
        transition: border-color 180ms ease, box-shadow 180ms ease;
      }

      .is-services .prose .faq-item[open] {
        border-color: rgba(var(--accent-tech-rgb), 0.38);
        box-shadow: 0 8px 18px rgba(2, 6, 23, 0.06);
      }

      .is-services .prose .faq-question {
        list-style: none;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
        margin: 0;
        padding: 13px 14px;
        font-size: 15px;
        font-weight: 760;
        line-height: 1.35;
        color: var(--text);
        cursor: pointer;
      }

      .is-services .prose .faq-question::-webkit-details-marker {
        display: none;
      }

      .is-services .prose .faq-question::after {
        content: "";
        width: 8px;
        height: 8px;
        border-right: 1.5px solid rgba(51, 65, 85, 0.95);
        border-bottom: 1.5px solid rgba(51, 65, 85, 0.95);
        transform: rotate(45deg);
        transition: transform 200ms ease-in-out;
        flex: 0 0 auto;
      }

      .is-services .prose .faq-item[open] .faq-question::after {
        transform: rotate(-135deg) translate(-1px, 1px);
      }

      .is-services .prose .faq-answer {
        padding: 0 14px 14px;
      }

      .is-services .prose .faq-answer > *:first-child {
        margin-top: 0;
      }

      .is-services .prose .faq-answer > *:last-child {
        margin-bottom: 0;
      }

      .is-services .prose .related-services-explorer {
        margin: 0 0 var(--space-xl);
        padding: var(--space-sm);
        border-radius: 14px;
        border: 1px solid rgba(var(--accent-tech-rgb), 0.28);
        background:
          radial-gradient(circle at 88% 12%, rgba(var(--accent-tech-rgb), 0.16), transparent 42%),
          radial-gradient(circle at 14% 88%, rgba(var(--primary-rgb), 0.12), transparent 40%),
          rgba(255, 255, 255, 0.98);
        box-shadow: 0 12px 26px rgba(2, 6, 23, 0.06);
      }

      .is-services .prose .related-services-intro {
        margin: 0 0 var(--space-xs);
        font-size: 13px;
        line-height: 1.55;
        color: rgba(51, 65, 85, 0.94);
      }

      .is-services .prose .related-services-grid {
        list-style: none;
        margin: 0;
        padding: 0;
        display: grid;
        gap: var(--space-2xs);
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .is-services .prose .related-services-grid li {
        margin: 0;
        padding: 0;
      }

      .is-services .prose .related-services-grid li::before {
        content: none;
      }

      .is-services .prose .related-service-link {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 8px;
        padding: 10px 11px;
        border-radius: 10px;
        border: 1px solid rgba(203, 213, 225, 0.92);
        background: rgba(255, 255, 255, 0.95);
        color: rgba(30, 41, 59, 0.96);
        text-decoration: none;
        font-size: 13px;
        line-height: 1.4;
        transition: border-color 170ms ease, background 170ms ease, transform 170ms ease;
      }

      .is-services .prose .related-service-link:hover {
        text-decoration: none;
        border-color: rgba(var(--accent-tech-rgb), 0.4);
        background: rgba(248, 250, 252, 0.96);
        transform: translateY(-1px);
      }

      .is-services .prose .related-service-link .material-symbols-rounded {
        font-size: 16px;
        opacity: 0.8;
      }

      .is-services .service-page-cta {
        margin-top: var(--space-lg);
        padding: clamp(16px, 2.4vw, 22px);
        border-radius: 14px;
        border: 1px solid rgba(var(--accent-tech-rgb), 0.3);
        background:
          radial-gradient(circle at 88% 18%, rgba(var(--accent-tech-rgb), 0.14), transparent 46%),
          radial-gradient(circle at 12% 90%, rgba(var(--primary-rgb), 0.11), transparent 44%),
          rgba(255, 255, 255, 0.98);
        box-shadow: 0 12px 28px rgba(2, 6, 23, 0.06);
      }

      .is-services .service-page-cta h2 {
        margin: 0 0 var(--space-xs);
        font-size: clamp(22px, 2.1vw, 30px);
        line-height: 1.18;
        letter-spacing: -0.02em;
      }

      .is-services .service-page-cta p {
        margin: 0 0 var(--space-sm);
        max-width: 76ch;
        color: rgba(30, 41, 59, 0.94);
      }

      .is-services .service-page-cta .home-cta {
        margin-top: var(--space-xs);
      }

      .is-services-index .prose {
        max-width: 100%;
      }

      .is-services-index .services-showcase {
        display: grid;
        gap: var(--space-xl);
      }

      .is-services-index .services-intro {
        position: relative;
        padding: clamp(18px, 2.8vw, 28px);
        border-radius: var(--radius);
        border: 1px solid rgba(203, 213, 225, 0.95);
        background:
          radial-gradient(circle at 86% 10%, rgba(var(--accent-tech-rgb), 0.16), transparent 44%),
          radial-gradient(circle at 16% 90%, rgba(var(--primary-rgb), 0.14), transparent 42%),
          rgba(255, 255, 255, 0.96);
        box-shadow: 0 14px 30px rgba(2, 6, 23, 0.06);
      }

      .is-services-index .services-intro h2 {
        margin: 0 0 var(--space-xs);
        font-size: clamp(24px, 2.7vw, 36px);
        line-height: 1.08;
        letter-spacing: -0.03em;
        color: var(--text);
      }

      .is-services-index .services-intro p {
        margin: 0 0 var(--space-sm);
        max-width: 78ch;
      }

      .is-services-index .services-intro .home-cta {
        margin-top: var(--space-sm);
      }

      .is-services-index .services-grid-showcase {
        display: grid;
        gap: var(--space-sm);
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .is-services-index .service-cluster {
        display: grid;
        gap: var(--space-xs);
        padding: var(--space-sm);
        border-radius: 14px;
        border: 1px solid rgba(203, 213, 225, 0.92);
        background: rgba(255, 255, 255, 0.96);
        box-shadow: 0 10px 22px rgba(2, 6, 23, 0.05);
      }

      .is-services-index .service-cluster-head {
        display: grid;
        gap: var(--space-2xs);
      }

      .is-services-index .service-cluster-kicker {
        margin: 0;
        font-size: 11px;
        font-weight: 800;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--accent-tech-dark);
      }

      .is-services-index .service-cluster-title {
        margin: 0;
        font-size: 20px;
        line-height: 1.2;
        letter-spacing: -0.02em;
      }

      .is-services-index .service-cluster-desc {
        margin: 0;
        color: var(--muted);
        line-height: 1.65;
      }

      .is-services-index .service-cluster-main {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        font-size: 13px;
        font-weight: 800;
        color: var(--accent-tech-dark);
      }

      .is-services-index .service-cluster-main:hover {
        text-decoration: underline;
      }

      .is-services-index .service-links {
        list-style: none;
        margin: 0;
        padding: 0;
        display: grid;
        gap: var(--space-2xs);
      }

      .is-services-index .service-links li {
        margin: 0;
        padding: 0;
      }

      .is-services-index .service-links li::before {
        content: none;
      }

      .is-services-index .service-links a {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 8px;
        border-radius: 9px;
        padding: var(--space-2xs) var(--space-xs);
        font-size: 13px;
        line-height: 1.45;
        color: rgba(51, 65, 85, 0.98);
        background: rgba(248, 250, 252, 0.9);
        border: 1px solid rgba(226, 232, 240, 0.95);
        text-decoration: none;
      }

      .is-services-index .service-links a:hover {
        color: var(--text);
        background: rgba(241, 245, 249, 0.95);
        border-color: rgba(var(--accent-tech-rgb), 0.34);
      }

      .is-services-index .service-links .material-symbols-rounded {
        font-size: 17px;
        opacity: 0.75;
      }

      .is-services-index .services-cta {
        display: grid;
        gap: var(--space-xs);
        padding: var(--space-sm);
        border-radius: 14px;
        border: 1px solid rgba(var(--accent-tech-rgb), 0.3);
        background:
          linear-gradient(160deg, rgba(var(--accent-tech-rgb), 0.13), rgba(255, 255, 255, 0.92) 55%),
          rgba(255, 255, 255, 0.95);
      }

      .is-services-index .services-cta h3 {
        margin: 0;
        font-size: 22px;
        line-height: 1.2;
        letter-spacing: -0.02em;
      }

      .is-services-index .services-cta p {
        margin: 0;
        max-width: 74ch;
      }

      .is-services-index .services-cta .home-cta {
        margin-top: var(--space-2xs);
      }

      .site-footer {
        border-top: 1px solid rgba(226, 232, 240, 0.9);
        padding: var(--space-lg) 0 var(--space-xl);
        color: var(--muted);
        font-size: 12px;
      }

      .footer-build {
        margin-top: 6px;
        font-size: 11px;
        color: #334155;
        letter-spacing: 0.01em;
      }

      .footer-build time {
        font-variant-numeric: tabular-nums;
      }

      .footer-grid {
        display: grid;
        grid-template-columns: minmax(0, 1.6fr) minmax(0, 1fr);
        gap: 18px;
        align-items: start;
      }

      .footer-title {
        font-weight: 800;
        color: var(--text);
        margin-bottom: 6px;
      }

      .footer-links {
        display: flex;
        flex-wrap: wrap;
        gap: 10px 14px;
        justify-content: flex-end;
      }

      .footer-links a {
        color: var(--muted);
        font-weight: 650;
      }

      .footer-links a:hover {
        color: var(--text);
        text-decoration: underline;
      }

      .home-hero {
        margin-top: calc(var(--header-height) * -1);
        padding: calc(var(--space-xl) + var(--header-height)) 0 var(--space-xl);
        min-height: min(372px, 46vh);
        display: flex;
        align-items: center;
        position: relative;
        background: var(--surface);
        overflow: visible;
        border-bottom: 1px solid rgba(226, 232, 240, 0.9);
      }

      .home-hero-bg {
        display: none;
      }

      .home-hero::before {
        content: none;
      }

      .home-hero::after {
        content: none;
        position: absolute;
        inset: 0;
      }

      .home-hero .container {
        position: relative;
        z-index: 3;
      }

      .home-hero-grid {
        display: grid;
        grid-template-columns: minmax(0, 1fr);
        gap: 24px;
        align-items: center;
      }

      .home-hero-visual {
        display: flex;
        align-items: stretch;
      }
      .home-hero-visual img {
        width: 100%;
        height: auto;
        border-radius: 12px;
        border: 1px solid #ccc;
        box-shadow:
          rgba(22, 31, 39, 0.42) 0px 60px 123px -25px,
          rgba(19, 26, 32, 0.08) 0px 35px 75px -35px;
        border-radius: 10px;
        border: 1px solid;
        border-color:
          rgb(213, 220, 226)
          rgb(213, 220, 226)
          rgb(184, 194, 204);
      }

      .visual-placeholder {
        width: 100%;
        min-height: 240px;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: var(--space-md);
        border-radius: var(--radius);
        border: 1px dashed rgba(var(--brand-blue-rgb), 0.3);
        background:
          radial-gradient(circle at top right, rgba(var(--highlight-rgb), 0.12), transparent 34%),
          linear-gradient(180deg, rgba(255, 255, 255, 0.96) 0%, rgba(241, 245, 249, 0.96) 100%);
        color: var(--muted);
        text-align: center;
        font-size: 13px;
        line-height: 1.6;
      }

      .visual-placeholder--hero {
        min-height: 320px;
      }

      .visual-placeholder--wide {
        min-height: 300px;
      }

      .visual-placeholder--compact {
        min-height: 180px;
      }

      .visual-placeholder--logos {
        min-height: 140px;
      }

      .visual-placeholder--feature {
        min-height: 420px;
      }

      .visual-placeholder--feature,
      .visual-placeholder--hero {
        border-radius: 12px;
      }

      .home-hero h1 {
        margin: 0 0 var(--space-xs);
        font-size: clamp(28px, 3vw, 40px);
        line-height: 1.03;
        letter-spacing: -0.05em;
        color: var(--text);
        text-shadow: none;
      }

      .home-hero h1 span {
        color: var(--text);
      }

      .home-lead {
        margin: 0 0 var(--space-md);
        max-width: 46ch;
        color: var(--muted);
        font-size: 15px;
        line-height: 1.55;
        text-shadow: none;
      }

      .home-cta {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: 12px;
        margin-top: var(--space-md);
      }

      .home-cta--hero {
        gap: 18px;
      }

      .cta-inline-link {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        color: var(--text);
        font-size: 15px;
        font-weight: 700;
        text-decoration: none;
      }

      .cta-inline-link svg {
        width: 16px;
        height: 16px;
        stroke: currentColor;
      }

      .cta-inline-link:hover {
        text-decoration: none;
        color: var(--cc-primary-hover);
      }

      .home-note {
        margin-top: var(--space-sm);
        color: var(--muted-soft);
        font-size: 12px;
        max-width: 42ch;
      }

      .hero-scroll {
        position: absolute;
        left: 50%;
        bottom: -18px;
        transform: translateX(-50%);
        z-index: 6;
      }

      .hero-scroll-link {
        display: inline-flex;
        flex-direction: column;
        align-items: center;
        gap: 6px;
        color: rgba(15, 23, 42, 0.9);
        text-decoration: none;
      }

      .hero-scroll-text {
        font-size: 10px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: rgba(15, 23, 42, 0.62);
      }

      .hero-scroll-icon {
        width: 44px;
        height: 44px;
        border-radius: 999px;
        border: 1px solid rgba(226, 232, 240, 0.92);
        background: rgba(255, 255, 255, 0.98);
        box-shadow:
          0 10px 24px rgba(2, 6, 23, 0.12),
          0 1px 0 rgba(255, 255, 255, 0.86) inset;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        animation: hero-scroll-bob 1.6s ease-in-out infinite;
      }

      .hero-scroll-icon svg {
        width: 18px;
        height: 18px;
        stroke: currentColor;
      }

      .hero-scroll-link:hover {
        text-decoration: none;
        color: #020617;
      }

      .hero-scroll-link:focus-visible {
        outline: 3px solid rgba(255, 255, 255, 0.74);
        outline-offset: 4px;
        border-radius: 999px;
      }

      @keyframes hero-scroll-bob {
        0%,
        100% {
          transform: translateY(0);
        }
        50% {
          transform: translateY(4px);
        }
      }

      @media (prefers-reduced-motion: reduce) {
        html {
          scroll-behavior: auto;
        }

        .hero-scroll-icon {
          animation: none;
        }
      }

      .home-hero .btn-ghost {
        border-color: rgba(255, 255, 255, 0.28);
        background: rgba(2, 6, 23, 0.18);
        color: rgba(255, 255, 255, 0.92);
      }

      .home-hero .btn-ghost:hover {
        background: rgba(2, 6, 23, 0.28);
      }

      .home-hero .btn-see-services:hover {
        background: rgba(2, 6, 23, 0.32);
      }

      .ai-loop {
        position: relative;
        --ai-loop-accent-rgb: var(--accent-tech-rgb);
        --ai-loop-accent: rgb(var(--ai-loop-accent-rgb));
        background: linear-gradient(180deg, rgba(255, 255, 255, 0.92) 0%, rgba(248, 250, 252, 0.92) 100%);
        border-bottom: 1px solid rgba(226, 232, 240, 0.9);
        overflow: hidden;
      }

      .ai-loop.section.compact {
        padding: var(--space-section-compact) 0;
      }

      .ai-loop::before {
        content: "";
        position: absolute;
        inset: 0;
        background:
          radial-gradient(circle at 18% 40%, rgba(var(--ai-loop-accent-rgb), 0.10), transparent 60%),
          radial-gradient(circle at 62% 52%, rgba(2, 6, 23, 0.06), transparent 58%),
          radial-gradient(circle at 86% 30%, rgba(var(--ai-loop-accent-rgb), 0.07), transparent 55%);
        pointer-events: none;
      }

      .ai-loop-grid {
        position: relative;
        display: grid;
        grid-template-columns: minmax(0, 1fr);
        gap: 14px;
        align-items: center;
      }

      .ai-loop-copy {
        max-width: 70ch;
      }

      .ai-loop-wrap .section-header {
        gap: var(--space-2xs);
        margin-bottom: var(--space-xs);
      }

      #ai-loop {
        scroll-margin-top: calc(var(--header-height) + 6px);
      }

      .ai-loop-lead {
        margin: 0 0 var(--space-xs);
        color: var(--muted);
        font-size: 14px;
        line-height: 1.75;
        max-width: 72ch;
      }

      .ai-loop-steps {
        list-style: none;
        padding: 0;
        margin: 0;
        display: grid;
        gap: 8px;
      }

      .ai-loop-step {
        width: 100%;
        text-align: left;
        display: grid;
        grid-template-columns: 22px minmax(0, 1fr);
        gap: 10px;
        align-items: start;
        padding: 12px 12px;
        border-radius: 14px;
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(255, 255, 255, 0.86);
        box-shadow: 0 10px 26px rgba(2, 6, 23, 0.05);
        cursor: pointer;
        transition: transform 140ms ease, border-color 140ms ease, box-shadow 140ms ease, background 140ms ease;
        color: inherit;
      }

      .ai-loop-step:hover {
        transform: translateY(-1px);
        border-color: rgba(var(--ai-loop-accent-rgb), 0.22);
        box-shadow: 0 14px 34px rgba(2, 6, 23, 0.07);
        background: rgba(255, 255, 255, 0.92);
      }

      .ai-loop-step:focus-visible {
        outline: 3px solid var(--ring);
        outline-offset: 2px;
      }

      .ai-loop-step-dot {
        width: 12px;
        height: 12px;
        border-radius: 999px;
        margin-top: 3px;
        background: rgba(148, 163, 184, 0.9);
        border: 2px solid rgba(255, 255, 255, 0.95);
        box-shadow: 0 10px 20px rgba(2, 6, 23, 0.12);
      }

      .ai-loop-step-title {
        margin: 0 0 2px;
        font-size: 13px;
        font-weight: 900;
        letter-spacing: -0.01em;
        color: var(--text);
      }

      .ai-loop-step-desc {
        margin: 0;
        font-size: 12px;
        line-height: 1.6;
        color: var(--muted);
      }

      .ai-loop-step.is-active {
        border-color: rgba(var(--ai-loop-accent-rgb), 0.32);
        background: rgba(var(--ai-loop-accent-rgb), 0.06);
      }

      .ai-loop-step.is-active .ai-loop-step-dot {
        background: rgba(var(--ai-loop-accent-rgb), 0.92);
      }

      .ai-loop-visual {
        display: none;
        justify-content: center;
        padding: 12px;
        overflow: visible;
      }

      .ai-loop-orbit {
        position: relative;
        width: min(360px, 100%);
        aspect-ratio: 1 / 1;
        border-radius: 999px;
        margin: 0 auto;
        overflow: visible;
      }

      .ai-loop-orbit svg {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
      }

      .ai-loop-center {
        position: absolute;
        inset: 50% auto auto 50%;
        transform: translate(-50%, -50%);
        width: min(240px, 72%);
        text-align: center;
        pointer-events: none;
      }

      .ai-loop-center-label {
        font-size: 11px;
        font-weight: 850;
        letter-spacing: 0.12em;
        text-transform: uppercase;
        color: rgba(71, 85, 105, 0.8);
        margin-bottom: 6px;
      }

      .ai-loop-center-step {
        font-size: 15px;
        font-weight: 900;
        letter-spacing: -0.015em;
        color: rgba(15, 23, 42, 0.96);
        line-height: 1.25;
      }

      .ai-loop-center.is-animating .ai-loop-center-label,
      .ai-loop-center.is-animating .ai-loop-center-step {
        animation: aiLoopCenterPop 260ms ease-out;
      }

      @keyframes aiLoopCenterPop {
        from {
          transform: translateY(6px);
          opacity: 0.55;
        }
        to {
          transform: translateY(0);
          opacity: 1;
        }
      }

      .ai-loop-track {
        stroke: rgba(148, 163, 184, 0.35);
        stroke-width: 8;
        fill: none;
        filter: drop-shadow(0 10px 16px rgba(2, 6, 23, 0.08));
      }

      .ai-loop-progress {
        stroke: rgba(var(--ai-loop-accent-rgb), 0.92);
        stroke-width: 8;
        fill: none;
        stroke-linecap: round;
        transform: rotate(-90deg);
        transform-origin: 50% 50%;
        transition: stroke-dashoffset 420ms ease;
        filter:
          drop-shadow(0 0 6px rgba(var(--ai-loop-accent-rgb), 0.26))
          drop-shadow(0 0 16px rgba(var(--ai-loop-accent-rgb), 0.14))
          drop-shadow(0 14px 18px rgba(var(--ai-loop-accent-rgb), 0.08));
        animation: aiLoopLineGlow 2.4s ease-in-out infinite;
      }

      .ai-loop-progress.is-resetting {
        transition: none;
      }

      @keyframes aiLoopLineGlow {
        0% {
          filter:
            drop-shadow(0 0 5px rgba(var(--ai-loop-accent-rgb), 0.18))
            drop-shadow(0 0 12px rgba(var(--ai-loop-accent-rgb), 0.10))
            drop-shadow(0 14px 16px rgba(var(--ai-loop-accent-rgb), 0.06));
        }
        50% {
          filter:
            drop-shadow(0 0 8px rgba(var(--ai-loop-accent-rgb), 0.30))
            drop-shadow(0 0 18px rgba(var(--ai-loop-accent-rgb), 0.16))
            drop-shadow(0 14px 22px rgba(var(--ai-loop-accent-rgb), 0.10));
        }
        100% {
          filter:
            drop-shadow(0 0 5px rgba(var(--ai-loop-accent-rgb), 0.18))
            drop-shadow(0 0 12px rgba(var(--ai-loop-accent-rgb), 0.10))
            drop-shadow(0 14px 16px rgba(var(--ai-loop-accent-rgb), 0.06));
        }
      }

      @media (prefers-reduced-motion: reduce) {
        .ai-loop-progress { animation: none; }
        .ai-loop-center.is-animating .ai-loop-center-label,
        .ai-loop-center.is-animating .ai-loop-center-step { animation: none; }
      }

      .info-grid {
        display: grid;
        grid-template-columns: repeat(4, minmax(0, 1fr));
        gap: 16px;
        margin-top: 18px;
      }

      .info-card {
        border-radius: 8px;
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(255, 255, 255, 0.9);
        box-shadow: none;
        padding: 20px;
      }

      .info-title {
        margin: 0 0 6px;
        font-size: 14px;
        font-weight: 850;
        letter-spacing: -0.01em;
        color: var(--text);
      }

      .info-body {
        margin: 0;
        color: var(--muted);
        font-size: 13px;
        line-height: 1.5;
      }

      .home-why {
        position: relative;
        overflow: hidden;
        background:
          linear-gradient(180deg, rgba(255, 255, 255, 0.98) 0%, rgba(248, 250, 252, 0.98) 100%);
      }

      .home-why::before {
        content: "";
        position: absolute;
        inset: 0;
        background:
          radial-gradient(circle at 12% 24%, rgba(217, 255, 0, 0.12), transparent 28%),
          radial-gradient(circle at 88% 78%, rgba(238, 242, 255, 0.95), transparent 26%);
        pointer-events: none;
      }

      .home-why .section-header {
        position: relative;
        margin-bottom: 18px;
      }

      .value-stack {
        display: grid;
        gap: 18px;
        margin-top: 24px;
      }

      .value-panel {
        min-height: clamp(420px, 44vw, 520px);
        display: grid;
        grid-template-columns: minmax(0, 0.88fr) minmax(0, 1.12fr);
        gap: clamp(28px, 5vw, 72px);
        align-items: center;
        padding: 44px;
        border-radius: 12px;
        border: 1px solid rgba(15, 23, 42, 0.08);
        background: rgba(255, 255, 255, 0.9);
        overflow: hidden;
      }

      .value-panel::before {
        content: "";
        position: absolute;
        inset: 0 auto 0 0;
        width: 6px;
        background: #e2e8f0;
      }

      .value-panel:nth-child(1)::before {
        background: #c7d2fe;
      }

      .value-panel:nth-child(2)::before {
        background: #d9ff00;
      }

      .value-panel:nth-child(3)::before {
        background: #fda4af;
      }

      .value-panel:nth-child(even) .value-panel-copy {
        order: 2;
      }

      .value-panel:nth-child(even) .value-panel-visual {
        order: 1;
      }

      .value-panel-copy {
        position: relative;
        max-width: 36ch;
      }

      .value-kicker {
        margin: 0 0 8px;
        font-size: 13px;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        color: var(--text);
      }

      .value-title {
        margin: 0 0 14px;
        font-size: clamp(32px, 3.6vw, 48px);
        line-height: 1;
        letter-spacing: -0.03em;
        color: var(--text);
      }

      .value-body {
        margin: 0;
        max-width: 32ch;
        font-size: 18px;
        line-height: 1.42;
        color: var(--muted);
      }

      .value-panel-visual .visual-placeholder {
        min-height: clamp(300px, 34vw, 420px);
        border: 1px solid rgba(15, 23, 42, 0.12);
        border-radius: 12px;
        background:
          linear-gradient(135deg, rgba(255, 255, 255, 0.82), rgba(255, 255, 255, 0.46)),
          rgba(255, 255, 255, 0.62);
        box-shadow: 0 10px 30px rgba(2, 6, 23, 0.08);
        color: rgba(15, 23, 42, 0.52);
      }

      .value-panel-visual img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }

      .pilot {
        border-radius: 8px;
        border: 1px solid rgba(var(--primary-rgb), 0.28);
        background: rgba(var(--primary-rgb), 0.06);
        padding: 28px;
        box-shadow: none;
      }

      .pilot-meta {
        margin: 0 0 var(--space-2xs);
        font-size: 12px;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 0.14em;
        color: var(--accent-tech-dark);
      }

      .pilot-title {
        margin: 0 0 var(--space-2xs);
        font-size: 20px;
        letter-spacing: -0.02em;
      }

      .pilot-body {
        margin: 0 0 var(--space-xs);
        color: var(--muted);
        font-size: 14px;
        line-height: 1.55;
        max-width: 54ch;
      }

      .pilot-list {
        margin: 0 0 var(--space-xs);
        padding-left: 18px;
        color: var(--muted);
        font-size: 13px;
        line-height: 1.55;
      }

      .partner {
        display: grid;
        grid-template-columns: auto minmax(0, 1fr);
        gap: var(--space-sm);
        align-items: center;
        border-radius: 8px;
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(255, 255, 255, 0.9);
        box-shadow: none;
        padding: 28px;
      }

      .partner-badge {
        display: flex;
        justify-content: flex-start;
      }

      .partner-badge img {
        width: 200px;
        height: auto;
        display: block;
        background: transparent;
        border: none;
        border-radius: 0;
        filter: drop-shadow(0 14px 24px rgba(2, 6, 23, 0.18));
      }

      .partner-kicker {
        margin: 0 0 var(--space-2xs);
        font-size: 12px;
        font-weight: 850;
        text-transform: uppercase;
        letter-spacing: 0.14em;
        color: var(--accent-tech-dark);
      }

      .partner-title {
        margin: 0 0 var(--space-2xs);
        font-size: 20px;
        letter-spacing: -0.02em;
        color: var(--text);
      }

      .partner-body {
        margin: 0 0 var(--space-xs);
        color: var(--muted);
        font-size: 14px;
        line-height: 1.55;
        max-width: 52ch;
      }

      .blog-header {
        display: grid;
        gap: var(--space-2xs);
        margin-bottom: var(--space-sm);
      }

      .blog-title {
        margin: 0;
        font-size: 28px;
        letter-spacing: -0.03em;
      }

      .blog-lead {
        margin: 0;
        color: var(--muted);
        max-width: 78ch;
        font-size: 14px;
        line-height: 1.75;
      }

      .blog-search {
        display: grid;
        gap: 8px;
        padding: 12px;
        border-radius: var(--radius);
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(255, 255, 255, 0.8);
        box-shadow: 0 8px 24px rgba(2, 6, 23, 0.04);
      }

      .blog-search-label {
        font-size: 12px;
        font-weight: 800;
        color: var(--muted);
        letter-spacing: -0.01em;
      }

      .blog-search-input {
        width: 100%;
        padding: 12px 12px;
        border-radius: 12px;
        border: 1px solid var(--border);
        font: inherit;
      }

      .blog-search-input:focus-visible {
        outline: 3px solid var(--ring);
        outline-offset: 2px;
      }

      .blog-search-meta {
        font-size: 12px;
        color: var(--muted-soft);
      }

      .blog-list {
        list-style: none;
        padding: 0;
        margin: var(--space-sm) 0 0;
        display: grid;
        gap: 12px;
      }

      .blog-grid {
        margin-top: var(--space-sm);
        display: grid;
        gap: 12px;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        align-items: stretch;
      }

      .blog-item {
        height: 100%;
      }

      .blog-more {
        margin-top: var(--space-xs);
      }

      .post-card {
        display: flex;
        flex-direction: column;
        gap: 8px;
        height: 100%;
        padding: 16px;
        border-radius: var(--radius);
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(255, 255, 255, 0.9);
        box-shadow: 0 8px 24px rgba(2, 6, 23, 0.06);
      }

      .post-card--compact {
        padding: 14px;
      }

      .post-card-thumb {
        width: 100%;
        height: 140px;
        border-radius: 14px;
        overflow: hidden;
        border: 1px solid rgba(226, 232, 240, 0.95);
        background: rgba(241, 245, 249, 0.9);
      }

      .post-card-thumb img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
      }

      .post-card-content {
        min-width: 0;
        display: flex;
        flex-direction: column;
        gap: 8px;
        flex: 1 1 auto;
      }

      .post-card-meta {
        margin: 0;
        color: var(--muted-soft);
        font-size: 12px;
        min-height: 1lh;
      }

      .post-card-title {
        margin: 0;
        font-size: 16px;
        letter-spacing: -0.02em;
      }

      .post-card-title a {
        color: inherit;
        text-decoration: none;
      }

      .post-card-title a:hover {
        text-decoration: underline;
      }

      .post-card-desc {
        margin: 0;
        color: var(--muted);
        font-size: 13px;
        line-height: 1.7;
        max-width: 78ch;
      }

      .post-card-more {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        margin-top: auto;
        font-size: 13px;
        font-weight: 800;
        color: var(--accent-tech-dark);
        text-decoration: none;
      }

      .post-card-more:hover {
        text-decoration: underline;
      }

      .blog-empty {
        margin-top: var(--space-xs);
        color: var(--muted);
        font-size: 13px;
      }

      .post-header {
        display: grid;
        gap: 10px;
        margin-bottom: var(--space-sm);
        max-width: 80ch;
      }

      .post-meta {
        margin: 0;
        color: var(--muted-soft);
        font-size: 12px;
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        align-items: center;
      }

      .post-meta-link {
        color: inherit;
        text-decoration: none;
        font-weight: 750;
      }

      .post-meta-link:hover {
        text-decoration: underline;
      }

      .post-title {
        margin: 0;
        font-size: clamp(28px, 3.6vw, 40px);
        line-height: 1.1;
        letter-spacing: -0.045em;
      }

      .post-lead {
        margin: 0;
        color: var(--muted);
        font-size: 14px;
        line-height: 1.75;
      }

      .post-figure {
        margin: var(--space-sm) 0 0;
        border-radius: var(--radius);
        overflow: hidden;
        border: 1px solid rgba(226, 232, 240, 0.95);
        box-shadow: 0 10px 30px rgba(2, 6, 23, 0.06);
        background: rgba(255, 255, 255, 0.9);
      }

      .post-figure img {
        width: 100%;
        height: auto;
        display: block;
      }

      @media (min-width: 860px) {
        .about-split {
          grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr);
          align-items: center;
        }

        .is-contact .prose .contact-layout {
          grid-template-columns: minmax(0, 1fr) minmax(0, 1.08fr);
          align-items: start;
        }

        .is-why-us .prose .whyus-intro {
          grid-template-columns: minmax(0, 1.25fr) minmax(0, 0.95fr);
        }

        .is-why-us .prose .whyus-pillars {
          grid-template-columns: repeat(3, minmax(0, 1fr));
        }

        .prose .usecase-table {
          table-layout: fixed;
        }

        .prose .usecase-kpi-grid {
          grid-template-columns: repeat(3, minmax(0, 1fr));
        }

        .about-photo img {
          aspect-ratio: 4 / 3;
          object-fit: cover;
        }
      }

      @media (max-width: 980px) {
        .is-lead-plan .leadplan-hero-image {
          display: none;
        }

        .is-lead-plan .leadplan-hero {
          grid-template-columns: 1fr;
          gap: var(--space-sm);
        }

        .is-lead-plan .leadplan-flow {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .services-grid {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .mega-menu {
          width: calc(100vw - 24px);
        }

        .mega-menu-groups {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .mega-menu-quickstart {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .footer-grid {
          grid-template-columns: minmax(0, 1fr);
        }

        .footer-links {
          justify-content: flex-start;
        }

        .info-grid {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .value-panel {
          grid-template-columns: minmax(0, 1fr);
        }

        .value-panel:nth-child(even) .value-panel-copy,
        .value-panel:nth-child(even) .value-panel-visual {
          order: initial;
        }

        .partner {
          grid-template-columns: minmax(0, 1fr);
        }

        .home-hero {
          padding-top: calc(var(--space-lg) + var(--header-height));
        }

        .blog-grid {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }
      }

      @media (max-width: 760px) {
        .is-lead-plan .leadplan-flow {
          grid-template-columns: 1fr;
        }

        .is-services-index .services-grid-showcase {
          grid-template-columns: minmax(0, 1fr);
        }
      }

      @media (max-width: 860px) {
        .nav,
        .header-actions {
          display: none;
        }

        .mobile-nav {
          display: block;
        }
      }

      @media (max-width: 560px) {
        .container {
          padding: 0 14px;
        }

        .home-cta .btn {
          width: 100%;
          justify-content: center;
        }

        .is-services-index .services-intro,
        .is-services-index .service-cluster,
        .is-services-index .services-cta {
          padding: var(--space-sm);
        }

        .mega-menu-groups {
          grid-template-columns: minmax(0, 1fr);
        }

        .mega-menu-quickstart,
        .prose .trust-grid {
          grid-template-columns: minmax(0, 1fr);
        }

        .services-grid {
          grid-template-columns: minmax(0, 1fr);
        }

        .table-scroll {
          overflow-x: auto;
        }

        .prose .usecase-kpi-grid {
          grid-template-columns: minmax(0, 1fr);
        }

        .conversion-compare {
          grid-template-columns: minmax(0, 1fr);
        }

        .is-why-us .prose .whyus-intro,
        .is-why-us .prose .whyus-pillars {
          grid-template-columns: minmax(0, 1fr);
        }

        .stats {
          grid-template-columns: minmax(0, 1fr);
        }

        .info-grid {
          grid-template-columns: minmax(0, 1fr);
        }

        .value-panel {
          gap: 24px;
          padding: 28px;
        }

        .blog-grid {
          grid-template-columns: minmax(0, 1fr);
        }

        .prose .visual-grid {
          grid-template-columns: minmax(0, 1fr);
        }

        .hero-scroll {
          bottom: -16px;
        }

        .hero-scroll-text {
          display: none;
        }
      }

      @media (min-width: 920px) {
        .home-hero-grid {
          grid-template-columns: minmax(0, 1.05fr) minmax(0, 0.95fr);
          gap: var(--space-xl);
          align-items: center;
        }

        .feature-panel {
          grid-template-columns: minmax(0, 0.82fr) minmax(0, 1.18fr);
          align-items: center;
          gap: 44px;
        }

        .feature-panel:nth-child(even) .feature-panel-copy {
          order: 2;
        }

        .feature-panel:nth-child(even) .visual-placeholder {
          order: 1;
        }

        .pricing-grid {
          grid-template-columns: repeat(3, minmax(0, 1fr));
          align-items: stretch;
        }

        .ai-loop-grid {
          grid-template-columns: minmax(0, 1fr) minmax(0, 440px);
          align-items: center;
        }

        .ai-loop-visual {
          display: flex;
          align-self: center;
        }
      }

    </style>
  </head>
  <body class="{% if isServices %}is-services{% endif %}{% if isServicesIndex %}{% if isServices %} {% endif %}is-services-index{% endif %}{% if isBlog %}{% if isServices or isServicesIndex %} {% endif %}is-blog{% endif %}{% if isContact %}{% if isServices or isServicesIndex or isBlog %} {% endif %}is-contact{% endif %}{% if isWhyUs %}{% if isServices or isServicesIndex or isBlog or isContact %} {% endif %}is-why-us{% endif %}{% if isLeadPlan %}{% if isServices or isServicesIndex or isBlog or isContact or isWhyUs %} {% endif %}is-lead-plan{% endif %}{% if isContent %}{% if isServices or isServicesIndex or isBlog or isContact or isWhyUs or isLeadPlan %} {% endif %}is-content{% endif %}">
    <a class="skip-link" href="#main">{% if isPolish %}Przejdź do treści{% else %}Skip to content{% endif %}</a>
    <div class="site">
      <header class="site-header{% if isHome %} site-header--overlay{% endif %}" aria-label="Primary navigation">
        <div class="container header-inner">
          <a href="{{ homeUrl }}" class="brand" aria-label="System Jawności Wynagrodzeń">
            <span class="brand-wordmark">System Jawności Wynagrodzeń</span>
          </a>

          <div class="header-actions">
            <a href="{{ signupUrl }}" class="btn btn-ghost">
              <span>{{ signupLabel }}</span>
            </a>
            <a href="{{ loginUrl }}" class="btn btn-primary">
              <span class="btn-icon btn-icon--portal" aria-hidden="true">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
                  <g fill="none" stroke="currentColor" stroke-dasharray="20" stroke-dashoffset="20" stroke-linecap="round" stroke-linejoin="round" stroke-width="2">
                    <path d="M6 19v-1c0 -2.21 1.79 -4 4 -4h4c2.21 0 4 1.79 4 4v1">
                      <animate fill="freeze" attributeName="stroke-dashoffset" dur="0.2s" values="20;0"/>
                    </path>
                    <path d="M12 11c-1.66 0 -3 -1.34 -3 -3c0 -1.66 1.34 -3 3 -3c1.66 0 3 1.34 3 3c0 1.66 -1.34 3 -3 3Z">
                      <animate fill="freeze" attributeName="stroke-dashoffset" begin="0.2s" dur="0.2s" values="20;0"/>
                    </path>
                  </g>
                </svg>
              </span>
              <span>{{ loginLabel }}</span>
            </a>
          </div>

          <details class="mobile-nav">
            <summary class="mobile-nav-trigger">
              <span class="mobile-nav-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <path d="M4 7H20" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
                  <path d="M4 12H20" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
                  <path d="M4 17H20" stroke="currentColor" stroke-width="2" stroke-linecap="round" />
                </svg>
              </span>
              <span>{{ menuLabel }}</span>
            </summary>
            <div class="mobile-nav-panel" role="dialog" aria-label="Mobile navigation">
              <a class="mobile-nav-cta" href="{{ signupUrl }}">
                <span>{{ signupLabel }}</span>
              </a>
            </div>
          </details>
        </div>
      </header>

      <main id="main">
        {% if isHome %}
          <section class="home-hero">
            <img
              class="home-hero-bg"
              src="/images/hero-image.jpg"
              alt=""
              aria-hidden="true"
              loading="eager"
              decoding="async"
              fetchpriority="high"
              width="1536"
              height="1024"
            />
            <div class="container">
              <div class="home-hero-grid">
                <div>
                  <h1>
                    Uporządkuj wynagrodzenia,
                    <span>zanim transparentność płac obnaży chaos</span>
                  </h1>
                  <p class="home-lead">
                    System Jawności Wynagrodzeń pomaga uporządkować stanowiska, widełki i zasady płac. Dzięki temu HR i zarząd mogą łatwiej wyjaśnić, skąd biorą się różnice w wynagrodzeniach.
                  </p>
                  <div class="home-cta home-cta--hero">
                    <a class="btn btn-primary" href="{{ signupUrl }}"><span>Załóż konto</span></a>
                  </div>
                  <div class="home-note">Dla firm, które chcą uporządkować wynagrodzenia, zanim transparentność płac zacznie tworzyć problemy.</div>
                </div>
                <div class="home-hero-visual">
                  <div class="visual-placeholder visual-placeholder--hero">
                    Podgląd ekranu głównego
                    <br />
                    dane -> poziomy -> widełki -> uzasadnienie
                  </div>
                </div>
              </div>
            </div>
          </section>

          <section class="section compact" aria-label="Stan obecny">
            <div class="container">
              <div class="section-header">
                <h2 class="section-title">Co dzieje się dziś bez systemu</h2>
                <p class="section-lead">Najczęściej dziś dane są rozproszone, decyzje są uznaniowe, a zasady wynagrodzeń nie są jasno opisane.</p>
              </div>
              <div class="feature-stack">
                <div class="feature-panel">
                  <div class="feature-panel-copy">
                    <p class="feature-eyebrow">01</p>
                    <h3 class="feature-title">Wynagrodzenia żyją w Excelach i wyjątkach</h3>
                    <p class="feature-desc">HR, managerowie i zarząd pracują na różnych wersjach danych. W praktyce nikt nie ma jednego, aktualnego obrazu stanowisk i płac.</p>
                  </div>
                  <div class="visual-placeholder visual-placeholder--feature">Podgląd: dane rozproszone między arkuszami</div>
                </div>
                <div class="feature-panel">
                  <div class="feature-panel-copy">
                    <p class="feature-eyebrow">02</p>
                    <h3 class="feature-title">Brakuje obiektywnych kryteriów</h3>
                    <p class="feature-desc">Firma nie ma wspólnej struktury stanowisk, poziomów ani prostych zasad, które tłumaczą różnice w wynagrodzeniach.</p>
                  </div>
                  <div class="visual-placeholder visual-placeholder--feature">Podgląd: poziomy i zasady przypisania</div>
                </div>
                <div class="feature-panel">
                  <div class="feature-panel-copy">
                    <p class="feature-eyebrow">03</p>
                    <h3 class="feature-title">Presja regulacyjna rośnie szybciej niż gotowość firmy</h3>
                    <p class="feature-desc">Im dłużej firma czeka, tym więcej ręcznej pracy, poprawek i trudnych rozmów o tym, skąd biorą się różnice w płacach.</p>
                  </div>
                  <div class="visual-placeholder visual-placeholder--feature">Podgląd: gotowość do transparentności wynagrodzeń</div>
                </div>
              </div>
            </div>
          </section>

          <section class="section compact home-why" aria-label="Dlaczego ten system">
            <div class="container">
              <div class="section-header">
                <h2 class="section-title">Co daje System Jawności Wynagrodzeń</h2>
                <p class="section-lead">To nie jest jednorazowe porządkowanie danych. To prosty sposób na uporządkowanie decyzji płacowych w całej firmie.</p>
              </div>
              <div class="value-stack">
                <div class="value-panel">
                  <div class="value-panel-copy">
                    <p class="value-kicker">Najważniejsze 01</p>
                    <h3 class="value-title">Jedno źródło prawdy o stanowiskach i płacach</h3>
                    <p class="value-body">Stanowiska, poziomy i widełki są w jednym miejscu, a nie w arkuszach, dokumentach i pojedynczych decyzjach managerów.</p>
                  </div>
                  <div class="value-panel-visual">
                    <div class="visual-placeholder">Podgląd: uporządkowana struktura stanowisk</div>
                  </div>
                </div>
                <div class="value-panel">
                  <div class="value-panel-copy">
                    <p class="value-kicker">Najważniejsze 02</p>
                    <h3 class="value-title">Obiektywne widełki i gotowe uzasadnienia</h3>
                    <p class="value-body">System pomaga ustalić poziomy, widełki i proste uzasadnienia różnic płacowych dla całej firmy.</p>
                  </div>
                  <div class="value-panel-visual">
                    <div class="visual-placeholder">Podgląd: widełki i uzasadnienie różnic</div>
                  </div>
                </div>
                <div class="value-panel">
                  <div class="value-panel-copy">
                    <p class="value-kicker">Najważniejsze 03</p>
                    <h3 class="value-title">Mniej ryzyka, mniej ręcznej pracy, większa kontrola</h3>
                    <p class="value-body">HR odzyskuje porządek, a zarząd i finanse lepiej kontrolują ryzyko, koszty wynagrodzeń i liczbę wyjątków.</p>
                  </div>
                  <div class="value-panel-visual">
                    <div class="visual-placeholder">Podgląd: kontrola i gotowość organizacji</div>
                  </div>
                </div>
              </div>
            </div>
          </section>

          <section class="section compact">
            <div class="container">
              <div class="section-header">
                <h2 class="section-title">Jak wygląda pierwszy efekt</h2>
                <p class="section-lead">Najważniejsze jest szybkie zobaczenie efektu: porządku, widełek i uzasadnień, bez długiego wdrożenia.</p>
              </div>
              <div class="feature-stack">
                <div class="feature-panel">
                  <div class="feature-panel-copy">
                    <p class="feature-eyebrow">Etap 01</p>
                    <h3 class="feature-title">Import danych</h3>
                    <p class="feature-desc">Na start wystarczą podstawowe dane o stanowiskach i wynagrodzeniach. Nie trzeba budować wszystkiego od zera.</p>
                  </div>
                  <div class="visual-placeholder visual-placeholder--feature">Podgląd: wczytanie danych o stanowiskach i płacach</div>
                </div>
                <div class="feature-panel">
                  <div class="feature-panel-copy">
                    <p class="feature-eyebrow">Etap 02</p>
                    <h3 class="feature-title">Wartościowanie i poziomy</h3>
                    <p class="feature-desc">System porządkuje role, poziomy i zasady przypisania, żeby decyzje płacowe nie opierały się na wyjątkach i uznaniowości.</p>
                  </div>
                  <div class="visual-placeholder visual-placeholder--feature">Podgląd: uporządkowanie poziomów i ról</div>
                </div>
                <div class="feature-panel">
                  <div class="feature-panel-copy">
                    <p class="feature-eyebrow">Etap 03</p>
                    <h3 class="feature-title">Widełki i uzasadnienie różnic</h3>
                    <p class="feature-desc">Na końcu dostajesz wynik, który pomaga wyjaśnić politykę płacową i przygotować firmę na pytania o wynagrodzenia.</p>
                  </div>
                  <div class="visual-placeholder visual-placeholder--feature">Podgląd: wynik i uzasadnienie różnic</div>
                </div>
              </div>
            </div>
          </section>

          <section class="section compact" aria-label="Dla kogo i obiekcje">
            <div class="container">
              <div class="section-header">
                <h2 class="section-title">Kto skorzysta na tym najbardziej</h2>
                <p class="section-lead">Największą wartość zobaczą firmy, które dziś działają na Excelu i chcą uporządkować płace zanim temat wynagrodzeń zacznie komplikować codzienną pracę.</p>
              </div>
              <div class="value-stack">
                <div class="value-panel">
                  <div class="value-panel-copy">
                    <p class="value-kicker">Dla HR</p>
                    <h3 class="value-title">Kiedy trzeba szybko uporządkować płace i odzyskać kontrolę</h3>
                    <p class="value-body">To rozwiązanie dla osób w HR, które dziś składają dane ręcznie, widzą niespójności w wynagrodzeniach i potrzebują jasnych zasad oraz widełek.</p>
                  </div>
                  <div class="value-panel-visual">
                    <div class="visual-placeholder">Podgląd: uporządkowana praca zespołu HR</div>
                  </div>
                </div>
                <div class="value-panel">
                  <div class="value-panel-copy">
                    <p class="value-kicker">Dla zarządu i finansów</p>
                    <h3 class="value-title">Gdy temat płac wymaga decyzji, a nie kolejnego arkusza</h3>
                    <p class="value-body">To dobre rozwiązanie dla zarządu i finansów, które chcą lepiej kontrolować ryzyko, koszty wynagrodzeń i sposób tłumaczenia różnic płacowych.</p>
                  </div>
                  <div class="value-panel-visual">
                    <div class="visual-placeholder">Podgląd: kontrola po stronie zarządu i finansów</div>
                  </div>
                </div>
                <div class="value-panel">
                  <div class="value-panel-copy">
                    <p class="value-kicker">Obiekcje</p>
                    <h3 class="value-title">To nie jest kolejny arkusz ani jednorazowy projekt</h3>
                    <p class="value-body">Jeśli firma chce tylko poprawić Excela na chwilę, to nie jest właściwe rozwiązanie. Ten produkt porządkuje zasady pracy na stanowiskach, widełkach i różnicach płacowych.</p>
                  </div>
                  <div class="value-panel-visual">
                    <div class="visual-placeholder">Podgląd: system zamiast kolejnych wyjątków</div>
                  </div>
                </div>
              </div>
            </div>
          </section>

          <section class="section compact" aria-label="Cennik">
            <div class="container">
              <div class="section-header">
                <h2 class="section-title">Cennik dopasowany do skali organizacji</h2>
                <p class="section-lead">Wybierz plan odpowiedni do wielkości firmy. Zaczynasz od prostszego wariantu, a zakres rośnie razem z organizacją.</p>
              </div>
              <div class="pricing-grid">
                <div class="pricing-card">
                  <div class="pricing-top">
                    <p class="pricing-kicker">Plan podstawowy</p>
                    <h3 class="pricing-title">Na dobry początek</h3>
                    <p class="pricing-price">od 2 000 zł <span class="pricing-period">rocznie</span></p>
                    <p class="pricing-note">Dla małych firm do 10 pracowników, które chcą zacząć od podstawowego uporządkowania stanowisk i wynagrodzeń.</p>
                  </div>
                  <ul class="pricing-list">
                    <li>do 10 pracowników</li>
                    <li>podstawowa struktura stanowisk i poziomów</li>
                    <li>widełki wynagrodzeń w jednym systemie</li>
                    <li>start bez ciężkiej integracji</li>
                  </ul>
                  <div class="pricing-actions">
                    <a class="btn btn-ghost" href="{{ signupUrl }}"><span>Wybierz plan</span>{% ms "arrow_forward" %}</a>
                  </div>
                </div>
                <div class="pricing-card pricing-card--featured">
                  <div class="pricing-top">
                    <p class="pricing-kicker">Najczęściej wybierany</p>
                    <h3 class="pricing-title">Plan rozwój</h3>
                    <p class="pricing-price">od 6 000 zł <span class="pricing-period">rocznie</span></p>
                    <p class="pricing-note">Dla firm od 11 do 50 pracowników, które chcą uporządkować zasady wynagrodzeń i przygotować się na nowe wymagania.</p>
                  </div>
                  <ul class="pricing-list">
                    <li>od 11 do 50 pracowników</li>
                    <li>więcej użytkowników i większy zakres stanowisk</li>
                    <li>szersze uzasadnienia różnic płacowych</li>
                    <li>lepsza kontrola i większa spójność między działami</li>
                  </ul>
                  <div class="pricing-actions">
                    <a class="btn btn-primary" href="{{ signupUrl }}"><span>Wybierz plan</span></a>
                  </div>
                </div>
                <div class="pricing-card">
                  <div class="pricing-top">
                    <p class="pricing-kicker">Plan rozszerzony</p>
                    <h3 class="pricing-title">Dla większej organizacji</h3>
                    <p class="pricing-price">od 12 000 zł <span class="pricing-period">rocznie</span></p>
                    <p class="pricing-note">Dla firm od 51 do 250 pracowników, które chcą uporządkować więcej ról, działów i zasad w jednym systemie.</p>
                  </div>
                  <ul class="pricing-list">
                    <li>od 51 do 250 pracowników</li>
                    <li>szerszy zakres organizacyjny</li>
                    <li>więcej kontroli i uporządkowania procesu</li>
                    <li>lepszy punkt wyjścia do pracy na większej skali</li>
                  </ul>
                  <div class="pricing-actions">
                    <a class="btn btn-ghost" href="{{ signupUrl }}"><span>Wybierz plan</span>{% ms "arrow_forward" %}</a>
                  </div>
                </div>
              </div>
            </div>
          </section>

          <section class="section compact" aria-label="System Jawności Wynagrodzeń next step call to action">
            <div class="container">
              <div class="partner lead-plan-panel">
                <div>
                  <p class="partner-kicker">Następny krok</p>
                  <h2 class="partner-title">Załóż konto i zacznij porządkować płace w jednym systemie</h2>
                  <p class="partner-body">Zacznij od danych o stanowiskach i wynagrodzeniach, a potem przejdź do poziomów, widełek i uzasadnień różnic. Pierwszy efekt powinien być widoczny szybko.</p>
                  <div class="home-cta">
                    <a class="btn btn-primary" href="{{ signupUrl }}"><span>Rejestracja</span></a>
                  </div>
                </div>
              </div>
            </div>
          </section>

          {% if faqSchema and faqSchema.length %}
            <section class="section compact" aria-label="FAQ">
              <div class="container">
                <div class="section-header">
                  <h2 class="section-title">{% if isPolish %}FAQ{% else %}FAQ{% endif %}</h2>
                  <p class="section-lead">{% if isPolish %}Najczęstsze pytania o dopasowanie, dane wejściowe, koszt i sposób startu z Systemem Jawności Wynagrodzeń.{% else %}Common questions about fit, input data, cost, and how to start with System Jawności Wynagrodzeń.{% endif %}</p>
                </div>
                <div class="prose">
                  {% for item in faqSchema %}
                    <h3>{{ item.question }}</h3>
                    <p>{{ item.answer }}</p>
                  {% endfor %}
                </div>
              </div>
            </section>
          {% endif %}

        {% else %}
          {% if renderPageHeader is defined and renderPageHeader == false %}
            {{ content | safe }}
          {% else %}
            <section class="page-hero">
              <div class="container">
                <nav class="page-breadcrumbs" aria-label="Breadcrumb">
                  <a href="{{ homeUrl }}">{{ homeLabel }}</a>
                  <span class="crumb-sep" aria-hidden="true">/</span>
                  {% if isServices and not isServicesIndex %}
                    <a href="/services/">{{ servicesLabel }}</a>
                    <span class="crumb-sep" aria-hidden="true">/</span>
                  {% elif isStart and page.url != '/start-with-ai/' %}
                    <a href="/start-with-ai/">{% if isPolish %}Start{% else %}Start with a Pilot{% endif %}</a>
                    <span class="crumb-sep" aria-hidden="true">/</span>
                  {% endif %}
                  <span aria-current="page">{{ pageHeading | escape }}</span>
                </nav>

                <div class="page-hero-inner">
                  <div class="page-header">
                    <h1>{{ pageHeading | escape }}</h1>
                    {% if description %}
                      <p class="page-lead">{{ description | escape }}</p>
                    {% endif %}
                  </div>
                </div>
              </div>
            </section>

            <section class="section">
              <div class="container">
                <article class="prose">
                  {{ content | safe }}
                </article>
                {% if isServices and not isServicesIndex %}
                  {% set ctaTitle = "Porozmawiajmy o tym, czy System Jawności Wynagrodzeń ma sens dla Twojej firmy" if isPolish else "Need help deciding whether System Jawności Wynagrodzeń fits your company?" %}
                  {% set ctaBody = "Powiedz nam, jak dziś wyglądają stanowiska, płace i sposób uzasadniania różnic, a pomożemy ocenić dopasowanie i sensowny pierwszy krok." if isPolish else "Tell us how compensation works today and we will help you evaluate fit and the right first step." %}
                  {% set ctaButton = "Rejestracja" if isPolish else "Sign up" %}
                  <section class="service-page-cta" aria-label="Service contact call to action">
                    <h2>{{ ctaTitle }}</h2>
                    <p>{{ ctaBody }}</p>
                    <div class="home-cta">
                      <a class="btn btn-primary" href="{{ signupUrl }}"><span>{{ ctaButton }}</span></a>
                    </div>
                  </section>
                {% endif %}
              </div>
            </section>
          {% endif %}
        {% endif %}
      </main>

      <footer class="site-footer">
        <div class="container footer-grid">
          <div>
            <div>© 2026, System Jawności Wynagrodzeń.</div>
            <div class="footer-build">
              Updated:
              <time datetime="{{ build.iso }}">{{ build.display }}</time>
            </div>
          </div>
          <div class="footer-links" aria-label="Footer links">
            <a href="{% if isPolish %}/pl/polityka-prywatnosci/{% else %}/privacy-policy/{% endif %}">{% if isPolish %}Polityka prywatności{% else %}Privacy policy{% endif %}</a>
            <a href="{% if isPolish %}/pl/polityka-cookies/{% else %}/cookie-policy/{% endif %}">{% if isPolish %}Cookies{% else %}Cookies{% endif %}</a>
          </div>
        </div>
      </footer>
    </div>
    <button
      class="cookie-fab"
      id="cookie-fab"
      type="button"
      aria-label="Cookie settings"
      hidden
    >
      {% ms "cookie" %}
    </button>
    <div
      class="cookie-banner"
      id="cookie-banner"
      role="dialog"
      aria-label="Cookie preferences"
      aria-live="polite"
      hidden
    >
      <div class="cookie-inner">
          <div>
            <p class="cookie-title">{% if isPolish %}Cookies{% else %}Cookies{% endif %}</p>
            <p class="cookie-text">
              {% if isPolish %}
                Używamy niezbędnych plików cookie, aby strona działała poprawnie. Za Twoją zgodą używamy także cookies analitycznych, aby rozumieć sposób korzystania ze strony i ją ulepszać.
                Swoją decyzję możesz zmienić w każdej chwili w ustawieniach cookies.
              {% else %}
                We use essential cookies to make this site work. With your permission, we also use analytics cookies to understand how the site is used and improve it.
                You can change your choice any time in Cookie settings.
              {% endif %}
            </p>
            <a class="cookie-link" href="{% if isPolish %}/pl/polityka-cookies/{% else %}/cookie-policy/{% endif %}">{% if isPolish %}Przeczytaj politykę cookies{% else %}Read our cookie policy{% endif %}</a>
            <div class="cookie-settings" id="cookie-settings" hidden>
              <div class="cookie-setting">
                <div>
                  <p class="cookie-setting-title">{% if isPolish %}Niezbędne{% else %}Necessary{% endif %}</p>
                  <p class="cookie-setting-desc">{% if isPolish %}Wymagane do działania strony.{% else %}Required for the site to function.{% endif %}</p>
                </div>
                <div class="cookie-switch is-on" aria-hidden="true" title="Necessary cookies are always on">
                  <span class="cookie-switch-thumb" aria-hidden="true"></span>
                </div>
              </div>
              <div class="cookie-setting">
                <div>
                  <p class="cookie-setting-title">{% if isPolish %}Analityczne{% else %}Analytics{% endif %}</p>
                  <p class="cookie-setting-desc">{% if isPolish %}Pomagają nam zrozumieć użycie strony i ją ulepszać (GA4).{% else %}Helps us understand usage and improve the site (GA4).{% endif %}</p>
                </div>
              <label class="cookie-switch" for="cookie-analytics-toggle">
                <input type="checkbox" id="cookie-analytics-toggle" aria-label="{% if isPolish %}Włącz analityczne pliki cookie{% else %}Enable analytics cookies{% endif %}" />
                <span class="cookie-switch-thumb" aria-hidden="true"></span>
              </label>
              </div>
            </div>
          </div>
        <div class="cookie-actions">
          <button class="btn btn-ghost" type="button" data-cookie-reject>{% if isPolish %}Odrzuć{% else %}Reject{% endif %}</button>
          <button class="btn btn-ghost" type="button" data-cookie-customize>{% if isPolish %}Dostosuj{% else %}Customize{% endif %}</button>
          <button class="btn btn-primary" type="button" data-cookie-accept>{% if isPolish %}Akceptuj cookies{% else %}Accept cookies{% endif %}</button>
        </div>
      </div>
    </div>
  {% if isHome %}
    <script src="/js/ai-loop.js" defer></script>
  {% endif %}
  {% if isServices and not isServicesIndex %}
    <script src="/js/faq-accordion.js" defer></script>
    <script src="/js/related-services.js" defer></script>
  {% endif %}
    <script src="/js/cookie-consent.js" defer></script>
  </body>
</html>
